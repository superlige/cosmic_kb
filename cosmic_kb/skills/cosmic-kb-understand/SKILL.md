---
name: cosmic-kb-understand
description: "理解和排查既有金蝶云苍穹（Cosmic/BOS）项目。用于回答字段被谁读取或修改、插件或方法在什么事件触发并执行什么业务、某操作影响哪些实体字段、字段是否有保存入库证据、单据引用/下推/审核回写链路，以及生成项目地图或接手分析。通过 cosmic_kb MCP 取证并读取本地源码，结论必须附类、方法、事件、文件行号及 confirmed/likely/unknown 置信度。不要用于安装 cosmic_kb、初始化或重建 KB、配置数据库元数据或诊断 MCP 连接；这些任务使用 cosmic-kb-setup。"
---

# Cosmic KB 项目理解

理解既有苍穹项目，不默认生成业务代码。使用 `cosmic_kb` MCP 获取确定性证据，读取本地源码补全上下文，并把结果整理成可复核的业务结论。

## 核心纪律

1. 先取证，后结论。不要按字段名、类名、包名或训练记忆猜测中文名、绑定关系、事件语义和保存结果。
2. 为每个判断标记 `confirmed`、`likely` 或 `unknown`，并说明依据。没有证据时明确写 `unknown`。
3. 通过 MCP 查询字段、单据、插件绑定和苍穹语义；直接读取本地源码全文，保留文件和行号。不要把源码、KB 或报告上传到公网站点。
4. 不要默认生成或改写 Java 业务代码。用户明确要求修改时，再把已确认的理解作为修改依据。
5. 把 `kd.bos.*`、`SaveServiceHelper` 等未知平台符号视为外部 SDK，按需调用 `cosmic_semantics` 核实，不要当成本项目缺陷。
6. 工具返回冲突、歧义或信息不足时不要自行挑选答案；缩小标识范围重新查询，仍无法确认则保留存疑。

## 工具使用原则

以各 MCP 工具当前的 schema、description 和 server instructions 为准，不在本 Skill 复制完整签名。

- 使用 `resolve_fields` 核对字段、实体、单据和插件类名。读到陌生标识时，先批量核对真实名称、所属坐标和取值语义。
- 使用 `trace` 追溯字段读取点、写入点、事件、保存证据和源码行号。
- 使用 `bill` 核对单据、操作集、插件绑定、启用状态和影响范围。
- 使用 `cosmic_semantics` 查询插件类型、事件时机、SDK、DynamicObject 路径、事务和入库判断。不确定主题时先空参列出主题，再按需读取一到两个。
- `trace` 或 `bill` 返回 `pagination.complete=false` 时，按照返回的 `next_cursor` 继续查询，直到 `complete=true`，再形成结论。
- 出现 `need_clarification`、`mismatched_*`、`invalid_request` 时，使用工具给出的候选或真实归属修正参数，不要绕过校验。
- 出现 `coarse_only`、`unlocated`、`dynamic_writers`、未绑定源码类或动态字段时，读取对应方法源码并把仍无法定位的部分单列为存疑。这些状态不等于“没有读写点”。

## 工作流

### 建立项目全貌

1. 确认 KB 和 MCP 已可用；若不存在、版本不符或未连接，停止取证并转用 `cosmic-kb-setup`。
2. 用户需要项目地图时，运行 CLI-only 的 `cosmic_kb report map`。
3. 用户需要规模、排障入口或风险热点时，运行 CLI-only 的 `cosmic_kb report overview`。
4. 从用户关注的模块、单据、字段或插件进入下列单点工作流，不要仅凭全貌报告推断字段行为。

### 字段追溯

1. 从源码上下文识别可能的单据、实体和字段坐标。
2. 用 `resolve_fields` 批量核对本轮出现的陌生标识；确认中文名、主实体/分录/子分录/基础资料路径，以及枚举或引用语义。
3. 用已确认的最精确坐标调用 `trace`。存在歧义时按候选重新查询，不要把裸字段的多个归属合并成一个结论。
4. 翻完所有分页，按源码类和方法整理读取点、写入点、事件与保存证据。
5. 对粗粒度命中、动态写入和未定位来源读取完整方法；仍缺保存链路时标为 `unknown`。
6. 按“字段排障结论”模板输出。

### 插件或方法解释

1. 只有插件类名时，先用 `resolve_fields` 的插件类型查询反查绑定单据；有单据标识后调用 `bill`。
2. 核对插件绑定的单据、操作和启用状态。`enabled=null` 只表示未确认，不要解释为已禁用。
3. 读取插件源码，定位触发方法、条件分支、字段读写和项目内服务调用。
4. 把源码出现的陌生字段、实体和单据标识批量交给 `resolve_fields` 核对。
5. 对不确定的插件事件、SDK 或保存语义调用 `cosmic_semantics`，不要复制训练记忆中的解释。
6. 翻完相关 `bill` 分页，按“插件/方法作用解释”模板输出。

### 操作影响分析

1. 使用 `bill` 获取目标单据的操作集和有效插件绑定，区分操作插件、页面插件、转换、反写和已禁用插件。
2. 读取与目标操作相关的插件源码，确认事务事件中实际读取或写入的实体字段。
3. 对跨单据创建、下推、反写和审核回写，继续核对目标单据与字段，不要把调用名称直接当成已落库证据。
4. 对每个受影响字段分别执行字段追溯，外层按“操作 → 影响字段”汇总。

## 回答格式

遵循“结论 → 证据 → 存疑 → 下一步”。省略与问题无关的栏目，但不要省略会改变可信度的存疑信息。

### 字段排障结论

```text
实体：<单据标识>（<中文名>）
字段：<字段标识>（<中文名>）｜路径：<主实体/分录/子分录/基础资料>

结论：<谁读取或修改、是否有入库证据、主要疑点>

写入点（按置信度排序；没有则写“未发现写入点”）：
  ✅ confirmed  <类>#<方法>  事件=<event>  行号=<file:line>  依据=<保存或事务证据>
  ~  likely     <类>#<方法>  事件=<event>  行号=<file:line>  依据=<间接证据>
  ?  unknown    <类>#<方法>  事件=<event>  行号=<file:line>  原因=<缺失证据>

未定位/存疑：
  - <歧义、动态写入、粗粒度命中、缺少保存链路等；没有则写“无”>

读取点：<仅在相关时列出类、方法和行号>
下一步建议：<具体到要查询的坐标、工具或源码位置>
```

### 插件/方法作用解释

```text
类：<全限定名>｜绑定：<单据/操作/启用状态>

结论：<业务作用及触发事件>

写入字段（按置信度排序；没有则写“无”）：
  ✅ confirmed  <字段标识（中文名）｜路径>  行号=<file:line>  依据=<保存或事务证据>
  ~  likely     <字段标识（中文名）｜路径>  行号=<file:line>  依据=<间接证据>
  ?  unknown    <字段标识（中文名）｜路径>  行号=<file:line>  原因=<缺失证据>

读取字段：<相关时列出>
调用的项目内方法/服务：<目标类或服务；没有写“无”>
未定位/存疑：<绑定、动态路径、事件或保存链路问题；没有写“无”>
风险点：<有证据支持的风险；没有写“无明显风险”>
下一步建议：<具体查询或源码位置>
```

## 完成检查

输出前确认：

- 所有陌生标识都已核对，未用字面含义替代元数据。
- 插件绑定来自 `bill` 或插件反查，不来自类名猜测。
- 所有相关分页均已完成。
- 每条关键结论都有工具结果或 `文件:行号`。
- 动态、粗粒度和未定位证据已单列，没有被解释成“不存在”。
- 入库结论符合 `cosmic_semantics` 的当前规则；证据不足时使用 `unknown`。

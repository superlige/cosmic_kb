# 苍穹历史项目本地理解工具 — 阶段验收台账

> 本台账每个阶段只记四块：**背景 / 目标 / 验收结论 / 相关命令**。
> 保持简洁——实施过程、返工记录、结构勘探、设计取舍**不进本文件**（留在代码注释、
> git 提交信息与 `开发计划.md`）。与 `开发计划.md`（计划蓝图）配套。

图例：✅ 已验收通过 ｜ 🔧 开发中 ｜ ⬜ 待开发

---

## 阶段 0 · 脚手架 + 资产复用 ✅

**背景**：起步搭项目骨架并复用已有苍穹文档/SDK 资产。

**目标**：`cosmic_kb/` 两段式子包骨架；CLI 入口；可编辑安装 + 可选依赖分组；冒烟测试。

**验收结论**：✅ 通过。`pip install -e .` 成功，`--version`/`doctor` 就位。

```powershell
pip install -e ".[dev]"; cosmic_kb --version; cosmic_kb doctor; pytest -q
```

---

## 阶段 1 · 源码摄取 + 解析可信度报告 ✅

**背景**：第一个信任门槛——证明能正确读取/解析"野生"源码（混合编码、不可编译、多模块）。

**目标**：递归摄取 `.java` + 编码自动探测 + tree-sitter 容错解析 + 覆盖率报告；`cosmic_kb ingest`。

**验收结论**：✅ 通过。真实项目 `asset_management_sys` 1,345 个 Java 文件，读取 100% /
解析干净 100% / 0 错误片段 / 无乱码。

```powershell
cosmic_kb ingest "<项目源码根>"
```

---

## 阶段 2 · 元数据解析 + 整包/多包处理 ✅

**背景**：元数据（dym/zip）是 KB 的另一半来源；一个 zip ≈ 一个业务模块、常多包并存。

**目标**：三类 dym（单据/基础资料/动态表单）统一为 `MetaModel`；hex oid 经模板回填；
整包双层 zip；多包跨模块（appKey）汇总。

**验收结论**：✅ 通过。单样例资产卡片 390 字段（层级无悬空）；整包单包 315 dym 0 失败、
多包目录 2 包 344 表单 0 失败。

```powershell
cosmic_kb meta "<dym|cr 或整包 zip>"     # 单 dym / 整包 / 目录多包汇总
```

---

## 阶段 3 · 元数据 ↔ 代码桥接 ✅

**背景**：把元数据插件的 `<ClassName>` 全限定名定位到源码 `.java`（只认全限定名、不靠 ISV/前缀猜）。

**目标**：以 `<ClassName>` 为唯一键映射；五态分类 + 孤儿收录；双前缀自动发现；孤儿打角色标签。

**验收结论**：✅ 通过。真实整包 project 插件命中率 91.1%、歧义 0、孤儿 1075（常量 207 + 真孤儿 868）。

```powershell
cosmic_kb bridge "<项目源码根>" "<dym|zip|目录>"
```

---

## 阶段 2/3 增补 · 转换规则 + 转换插件桥接 + 插件基类孤儿 ✅

**背景**：要看懂单据流转。新增一类元数据**转换规则**（`.cr`）记源→目标单据并绑转换插件；
"继承插件基类却未绑定"的类是死代码/元数据未给全的信号，需单独区分。

**目标**：解析 `.cr`（上下游/字段映射/转换插件）；转换插件纳入桥接；插件基类孤儿传递闭包识别；
图谱增 `convert_rule` 表 / `converts_to` 边 / `plugin_base` 列。

**验收结论**：✅ 通过。`samples\trans` 56 条转换规则全解析、35 条绑插件；插件基类孤儿闭包识别正确。

```powershell
cosmic_kb meta samples\trans\<包>.zip
cosmic_kb bridge "<源码根>" samples\trans
```

---

## 阶段 4 · 知识图谱存储 + 项目地图 / 理解报告 ✅

**背景**：把阶段 1-3 的内存产物沉淀进可持久、可重建、可查询的 KB（段一段二的契约）。

**目标**：SQLite 图谱（节点 + edge + FTS5）幂等重建；多信号模块识别（主锚 appKey、包前缀辅证、
带一致度）；一键理解报告（概览/模块/实体/插件/风险热点）；`build` / `report map` / `report overview`。

**验收结论**：✅ 通过。真实库重建 module 2 / form 344 / field 6671 / plugin 703 / source_class 1349；
模块识别包结构一致度 99%、跨模块散落如实点名；幂等重建计数一致。

```powershell
cosmic_kb build "<项目源码根>" "<dym|zip|目录>"
cosmic_kb report map; cosmic_kb report overview
```

---

## 阶段 4.5 · 本地 Web 展示层 ✅

**背景**：把项目地图/理解报告搬进浏览器，更直观浏览。纯表现层、复用 KB、不改 schema。

**目标**：本机 `localhost` 服务（仅绑 127.0.0.1、不引 CDN）；标准库 `http.server` 零新依赖；
`/api/*` 复用现成 report 函数；最小单页（概览/地图/表单/风险/搜索）。

**验收结论**：✅ 通过。真实 KB 端到端各端点正常、桥接命中率与阶段 4 一致；含路径穿越防护。

```powershell
cosmic_kb web --open
```

---

## 阶段 5+6+7 · 字段级排障引擎（旗舰）✅

**背景**：产品方向从「项目普查」重定向为「字段级排障导航」（用户 2026-06-17 拍板）——消灭
苍穹排障痛点「把元数据里一堆插件全路径逐个复制到代码里翻」。

**目标**：输入字段标识 → 列出所有读/写它的**插件 + 事件函数 + 是否落库 + 行号 + 源码路径**，
按**实体坐标 (单据·层级·分录)** 分组精确定位。一次实现 阶段 5（事件/字段）+ 6（调用链，
类内 + 跨类回溯）+ 7（落库判定 = 事件相位 × 操作类型 × 调用链到 sink）。

**验收结论**：✅ 通过（经四轮返工至用户验收）。
- Java 语义层 `java/`（ast_index/constants/plugin_classifier/event_extractor/field_access/
  call_graph/persistence/project_graph/analyze），全 tree-sitter 容错、零硬依赖。
- 数据包来源识别覆盖：事件入参（单包/数组）、ORM load/loadSingle/query、转换插件目标/源单、
  getEntryEntity、for-each、lambda 行变量、stream 派生集合、DynamicObject 入参跨类传播。
- 查询入口按层级显式录入（点号段数定层级）；判不准进「可能命中」、来源判不出进「未定位」，不遗漏。
- 未绑定的苍穹插件（AbstractTask/WebApi/工作流）也作跨类入口回溯。
- KB schema **v7**（`field_access` 增 `access_class` + 坐标索引）；报告 `field_trace` + `bill_view`；Web 重做单页。
- 真实库复验 `cqkd_assetcollateral.cqkd_collateralstatus`：CollateralOp + CollateralService +
  AutoExecuteCollateralTask 写入全部精确命中、落库、带跨类调用链 + 源码行号。

```powershell
cosmic_kb trace cqkd_assetcard.cqkd_entry.cqkd_amount   # 点号精确定位
cosmic_kb bill cqkd_assetcard                            # 单据钻取
```

---

## 信任优先 · 可信度报告（手段一覆盖率 + 手段二粗/高精度对比）✅

**背景**：红线 #4「覆盖率/可信度是一等功能」。接手者要先信任：①扫描覆盖了多少字段？
②高精度扫描会不会静默漏字段？

**目标**：手段一 `coverage`（元数据业务字段为分母算覆盖率 + 四维质量分解）；手段二 `scan-compare`
（高精度 `field_access` vs 粗精度正则字面量，分桶：两者都见 / 仅粗扫见=疑似盲点 / 仅高精度见=精度增量）。

**验收结论**：✅ 通过。真实库业务字段 3255、两者都见 1547、仅粗扫见 829（强信号盲点 11 带行号）、
仅高精度见 0。KB 增 `coarse_field_hit` 表（**schema v8**）；Web「扫描可信度」页签接续。

```powershell
cosmic_kb coverage; cosmic_kb scan-compare
```

---

## 阶段 8 · 业务流分析 ⬜（已拍板搁置）

2026-06-22 拍板：不单独做，折进阶段 9 按需——业务流上下文只用现成 BOTP 边
（`convert_rule`/`converts_to`），引用/审核回写无数据则留 unknown。

---

## 阶段 9 · 语义解析 + Context Builder + Skill 集成 ✅

**背景**：阶段 5-7 要求背精确字段标识，但接手者多半只记中文名。让用户用自然语言提问，工具
**确定性**解析成 KB 查询。`ask` 产出确定性证据包（不调 LLM），推理交段二 Skill。

**目标**：`dictionary`（中文名↔标识词典 + 模糊候选）；`resolver`（意图分类 + 低置信反问，不替用户拍板）；
`builder`（按意图复用取证函数组装证据包，dict + 文本双输出）；`cosmic_kb ask`（消歧退出码 3）；更新 SKILL.md。

**验收结论**：✅ 通过。真实库 `ask` 旗舰与 `trace` 同口径；单据钻取/插件解释正确；同名歧义列候选反问。

```powershell
cosmic_kb ask "<自然语言问题>"          # 消歧退出码 3，--json 喂 Skill
```

---

## 阶段 10 · MCP 封装（段二大模型接入）✅

**背景**：把取证能力交给 LLM 宿主。按红线 #6 走 MCP Server：只把取证命令包成工具，宿主挂 Skill
后自己调工具取证、自己推理；工具每次只回最小证据包，源码全文由宿主大模型直接读本机文件做完整理解。

**目标**：`mcp/server.py`（FastMCP + 取证工具，返回值与 CLI `--json` 同口径、零重写、未装 `[mcp]`
也可 import）；`cosmic_kb mcp`（stdio）+ `cosmic_kb-mcp` 入口 + `.mcp.json`；SKILL.md 补 MCP 节。

**验收结论**：✅ 通过。装 `[mcp]` 后 `build_server` 注册出 `ask/trace/bill/method_calls/coverage/
scan_compare` 工具、schema 正确；`cosmic_kb mcp` stdio 启动正常；顺带修复单据钻取 NULL field_key 崩溃。

```powershell
pip install -e ".[mcp]"; cosmic_kb mcp --db cosmic_kb.db
# Claude Code：项目根 .mcp.json 自动识别，批准后直接自然语言提问
```

---

## 阶段 10 增补 · 方法出向调用导航 method_calls ✅

**背景**：原 `read_method`（方法级深读：复述方法源码 + 语义摘要）在交付人工测试时被否——段二
形态拍板为**大模型直接读本机源码 + 挂苍穹 skill**，复述源码 / 列平台·`equals`·常量调用 / 做自然
语言解释这些大模型自己做得更好，静态层在这块零增量甚至刷屏噪声（如 `super.propertyChanged()`、
`e.getProperty()`、常量类 `equals` 全被列成调用）。确定性扫描层对一个会读源码的大模型唯一不可
替代的，是**野生、多 ISV 前缀、不可编译码上的"跳转到定义"**。故把方法级能力收窄重置为纯调用导航。

**目标**：给定 类全限定名 + 方法名 → 只回该方法调用的**项目内**方法（调用名 + 目标类全限定名
`target_fqn` + 目标源文件 `target_relpath` + 调用行号），供大模型顺调用链逐层读源码下钻。**不回
源码全文**（大模型自读）、**不列平台/外部/落库 sink 调用**（噪声）、**不做字段落库取证**（归
`trace`）；接收者类型解不出 → 不收录（宁缺毋滥，不臆造）。schema 不变 v9。四入口：CLI `calls`
+ MCP 工具 `method_calls` + `ask` 意图 `method_calls` + `trace`/`bill` 跳转提示。

**验收结论**：⏳ 待人工测试。`pytest -q` **180 passed**（`tests/test_method_calls.py` 9：项目内调用
解析+噪声不收 / 跨类下钻 / 源码根自动解析 / 三类未命中候选 / resolver+builder+MCP 同口径）。诚实
边界：tree-sitter 未装或源码读不到 → 给空清单 + 明确提示、不臆造；同名类/重载列候选反问。

```powershell
cosmic_kb calls "<类全限定名>" "<方法名>"   # 该方法调了项目内哪些方法→目标类/源文件/行，供顺调用链读源码下钻
```

---

## 分发地基 · 自包含包重构 + MCP 语义下沉 ✅

**背景**：`docs/分发与多agent接入方案.md` 拍板「把工具变成标准自包含 MCP 包再搭现成车（PyPI/uvx +
`.mcpb` + Smithery）」。两个死结挡在前面，是一切分发的公共前置：① `_assets.py` 用 `parents[1]` 把
`comic-understand-long/`、`skill_assets/`、`samples/bos_temp/` 当 `cosmic_kb/` 同级目录，逼着整仓
`pip install -e .`，当不了普通 wheel；② 段二语义层只绑在 Claude 私有 SKILL，非 Claude agent 注册 MCP
后只会裸调、拿不到苍穹纪律。本次只做这两步前置改造，发布上车另起会话。

**目标**：① 语义文档（references/rules）与继承根模板下沉进包（`cosmic_kb/semantics/`、
`cosmic_kb/metadata/templates/`），`_assets.py` 改 `importlib.resources` 定位、去 `parents[1]`；
9MB `ok-cosmic-docs.db` 运行期零消费 → 踢出 wheel 转可选（`COSMIC_KB_DOCS_DB`），doctor 标 OPTIONAL。
② MCP server 加 `instructions`（跨 agent 注入路由+三态纪律）+ 新工具 `cosmic_semantics(topic)`（按主题
回传随包语义文档单一源）。顶层 SKILL.md 瘦身为指向 `cosmic_semantics`，skill 脚本 refs 重定向到包内。

**验收结论**：✅ 通过。`pytest -q` **187 passed**（新增 `test_assets_resources.py` + semantics/instructions
用例）。硬验收：`python -m build` 出 wheel（42 篇语义 md 含嵌套 adv/plugin/sdk + 2 模板进包、9MB DB 正确
排除）→ **全新 venv `pip install`（非 -e）→ 非仓库目录可读全部资产**，`parents[1]` 依赖已断；`doctor`
semantics/templates OK、db OPTIONAL、退出码 0；装 `[mcp]` 后 `build_server().instructions` 非空、
`cosmic_semantics('')` 列主题、命中回单篇全文、7 工具齐（含 `cosmic_semantics`）。

```powershell
python -m build --wheel                          # 出自包含 wheel（assets 随包）
pip install dist\cosmic_kb-<版本>-py3-none-any.whl   # 非 -e 装进 site-packages 也能读到资产
cosmic_kb doctor                                 # semantics/templates OK；ok-cosmic-docs.db OPTIONAL
```

---

## 阶段 10 增补 · unknown 字段诚实分类 + 动态写入交段二读源码 ✅

**背景**：`trace` 按**解析出的字段 key 字面值**查 `field_access`，`field_key=None` 的访问对按字段查询
**完全隐形**。真实项目 `asset_management_sys` 实测 19079 条里 955 条 key 钉不出（unknown 906/ambiguous
31/dynamic 18）。读真实源码定性：这 906 条**绝大多数不是工具漏跟了常量，而是代码对运行时/配置/元数据
决定的动态字段集做泛化读写**（`for(f:coll)set(f,..)`、`map.forEach`、`CON.X+"_"+v`），静态原理上钉不出
唯一字段，硬展开既误报又脆弱。用户 2026-06-24 拍板：确定性层**不解释、不展开、不调大模型**，只分类 +
亮清单 + 收集导航证据，"循环到底碰哪些字段、是否含 X"交段二大模型直接读源码（红线 #1/#4/#6）。

**目标**：① 把 null-key 写入按成因细分 `key_resolution`（`dynamic-loop`/`concat`（含已知前缀段抽取）/
`external-const`/`unknown`，field_key 仍为空不臆造）；② `trace X` 折进「动态写入候选」段，**按同单据
(form_key)+层级限定范围**；③ 全局审计 `dynwrites`（CLI + MCP 工具 `dynamic_writes`，支持 form/cause/class
过滤）；④ **防上下文爆炸**：trace 与 dynwrites 一律**按 (入口类,事件方法) 去重成「该读方法」清单**
（cap 10，每条带 calls 导航 + 物理写入位置），不回逐行。schema 不变 v9。

**验收结论**：✅ 通过（用户审阅真实 KB 的 dynwrites/trace 输出后确认）。`pytest -q` **191 passed**
（新增 `tests/test_dynamic_writes.py` 4：四态分类不臆造 / 全局方法去重 worklist / cause 过滤 / trace 折进）。
真实数据：906 不透明 unknown 拆为 unknown 612 + concat 260 + dynamic-loop 207 + external-const 76（约 540
条得明确成因 + 锚点）；按方法去重压缩约 13×（trace 某字段 52 处→3 个方法、全局 207 处→56 个方法）。回归
审计：`coverage`/`scan-compare`/Web 对新 key_resolution 取值无枚举假设、自动兼容。诚实边界：来源单据
未定位（form_key 空）的动态写入只进全局清单、不挂 trace 单字段段（数据流没追到来源的固有缺口）。

```powershell
cosmic_kb dynwrites                          # 全局审计：null-key 读写按成因桶 + 去重方法清单（防爆上下文）
cosmic_kb dynwrites --form <单据> --cause concat --cls <类>   # 缩范围切片
cosmic_kb trace "<单据.字段>"                 # 末尾「动态写入候选」段：同单据该读哪几个方法 + calls 锚点
```

---

## 阶段 10 增补 · 字段名核对 `resolve_fields` ✅

**背景**：段二大模型读 Java 源码时靠**命名惯例猜字段中文名**翻车——`cqkd_zjjnqk` 被猜成"资金缴纳情况"，
真实是"租金缴纳情况"（一字之差），整轮解释没调过任何取证工具核对。根因两件事同时缺位：① 没有"便宜"的
字段名查询工具（现有 `trace`/`bill`/`ask` 都"重"：payload 大、语义是"谁改了它"而非"它叫什么"，模型不愿
为核一个名去调，于是走阻力最小的路：猜）；② MCP `INSTRUCTIONS` 没有硬反射（只说"绝不臆造"，但模型主观
把"按命名惯例推断"当识别而非臆造）。用户 2026-06-25 拍板：补轻量工具 + 加纪律，把"按命名惯例臆断"挤掉。

**目标**：① 新增 O(1) 取证 `resolve_fields(keys)`——标识 → 真实元数据中文名+实体坐标，**同时覆盖
`field`+`entity` 两表**（分录容器 key 也能解析，避免读到 `getDynamicObjectCollection("cqkd_zdfl")` 解不出），
同 key 跨多坐标全摆出**不替选**，**钉不出回 `null` 不臆造**；② 复用词典层 `Lexicon`（补 `entities_by_key`
索引），纯逻辑 `report/resolve_fields.py`，CLI/MCP 同口径，零 schema 改动；③ MCP `INSTRUCTIONS` 加
「字段名纪律」一条（引用 `<isv>_前缀` 字段并陈述其中文名前，必须先 `resolve_fields` 比对，命名惯例不算
证据，回 null 标 unknown）。Read 钩子自动提醒（方案 §2.3）确认暂不做（留作后续可选）。

**验收结论**：✅ 通过。`pytest -q` **201 passed**（新增 `tests/test_resolve_fields.py` 8：字段名解析 /
同 key 多坐标全摆出 / 分录容器 key 解析 / 钉不出回 None / 批量混合 / 文本标 null / MCP 同口径 / CLI --json；
更新 test_mcp.py TOOLS 注册表断言）。返回形状 `{"resolved": {key: [item]|null}}`，item 自带 `kind` 判别
（field / entry / subentry / header）：字段回 `{name,form_key,entity_key,level,field_kind}`，容器回
`{name,form_key,level,parent_key}`。**配套修复**：`entity.parent_key` 曾误存父实体 **oid**
（验收时发现 `cqkd_zdfl` 的 parent_key 返回 `1B+5Q7IXAJGI`），`store.py` 改用本单据 oid→key 映射翻成
父实体 **key**（`bill` 钻取同受益）；schema 不变，**需 `cosmic_kb build` 重建 KB 生效**。

```powershell
cosmic_kb resolve cqkd_zjjnqk cqkd_zdfl              # 批量核对：标识→真实中文名+坐标（钉不出回 null）
cosmic_kb resolve "<字段标识>" --json                # 机器可读，喂段二
```

---

## 阶段 10 增补 · 模式 B 语义增强（把核对结果焊进工具返回值）✅

**背景**：上一节加了 `resolve_fields` + `INSTRUCTIONS`「字段名纪律」后，2026-06-25 一次真实段二调用显示
模型**仍绕过** `resolve_fields`/`cosmic_semantics`：凭命名惯例猜字段名、凭训练知识臆断 `propertyChanged`
的触发时机与是否入库，整轮没核对。关键发现——模型**知道**规则（能准确说出该查 `plugin-form`、该
`resolve_fields`），但还是没执行：`INSTRUCTIONS` 是一次性系统提示常量，离决策时刻远，压不过自信先验；且
模型"读源码→直接答"的默认回路**一个取证工具都不碰**，软约束施加的位置不对。结论：不该再去"强制模型
调用"（对自主 agent 几乎不可达），应换目标。用户拍板：段二是"用户直接开 agent 聊、且不一定是 Claude
Code"，故宿主钩子（§2.3）不可移植——**通用 host 上唯一所有客户端都必读的硬信息，是我们自己工具的返回值**。

**目标**：模式 B——不强制调用，把"已核对的事实"**焊进模型本来就要走的导航工具返回里**，让它必定读到、
想按命名惯例改写都没机会：① 字段旁内联 `field_name` 已核对中文名（`trace` 顶层 + `bill` 的 `field_touch`
+ `ask` 插件/操作证据；覆盖 field+entity 两表；钉不出留 `None` 不臆造）；② 事件方法旁内联 `semantics_topic`
（`propertyChanged`→`plugin-form`、`beforeDoOperation`→`plugin-operation`…），提示"判触发时机/是否入库前
先 `cosmic_semantics(topic)`，勿凭训练知识臆断"；③ 新增中立复用模块 `semantic/hints.py`（事件→主题映射 +
字段名索引，report/context/method_calls 复用、不反向依赖 mcp）；④ `INSTRUCTIONS` 加「返回值已带证据，
直接采用」。host-agnostic，零 schema 改动（v9）。

**验收结论**：✅ 通过。`pytest -q` **211 passed**（新增 `tests/test_hints.py` 10：事件→主题映射 / 回落
plugin_type / 提示串 / 字段名索引覆盖 field+entity / 同 key 多名歧义留 None 不替选 / trace 顶层名+写入行
topic / bill `field_touch` 名+事件 topic / ask 插件解释名+topic / ask 操作解释名 / method_calls 事件方法
带 topic）。**已知天花板（诚实记录）**：若模型既不调任何导航工具、又用宿主原生 reader 纯读源码后直接答，
这条全绕过的路 host-agnostic 手段一个都拦不住——那一截缝留待**模式 A**（自带自动标注的 `read_source`
工具，用野生编码正确解码等硬价值把原生读拉回来）后续评估。

```powershell
cosmic_kb trace "<单据.字段>"     # 写入行已带 ⚑ 事件语义路由（cosmic_semantics('plugin-...')）
cosmic_kb bill "<单据>"           # 字段触达已带「中文名」+ 事件 semantics_topic
cosmic_kb ask "<某类/某操作>"     # 证据包字段带 field_name、事件带 semantics_topic
```

---

## 阶段 10 增补 · 堵两条"读源码猜字段名"路径：method_calls 带字段 + read_source（模式 A）✅

**背景**：模式 B 落地后，2026-06-25 第二次真实样本仍翻车——模型走的路径是「`method_calls`（方法导航，
**不带字段名**）→ 宿主原生 reader 直接读源码 → 字段 key（`KEY_TZRQ`/`cqkd_zdfl`）只在源码正文里出现、
从没进过任何工具返回值 → 按拼音猜」，并**主动选择省事跳过 `resolve_fields`**。这正是模式 B 的覆盖天花板：
内联只在模型流经带名工具（trace/bill/ask）时生效，而这条路一个都没碰。用户拍板「两个都做，先 method_calls
再模式 A」。

**目标**：① **method_calls 延伸模式 B**——`field_access` 无"访问方法"列，但有 `source_relpath`+`line`，故按
方法 AST 行范围 `start_line..end_line` 圈出该方法体读写的字段，返回 `fields`（已核对中文名 + 是否落库 +
事件语义路由），导航到方法就拿真名；钉不出具体字段的动态写入只计数（交 dynwrites/读源码定性）。② **新增
`read_source`（模式 A）**——让模型读源码走我们的工具而非宿主原生 reader：按建库同款编码探测**正确解码**野生
码（GBK/GB2312/UTF-8±BOM，原生 reader 易乱码=拉它过来的硬理由）、行号对齐 KB，并**自动标注** `field_names`
（扫文本里出现的已知字段/容器 key→真实中文名；常量 `KEY_X="cqkd_x"` 的字面值就在源码正文，无需常量表，
直接复用 `resolve_fields`）；支持 `--lines A-B` 区间、防 `../` 越界。新增 `report/source_read.py`（源码读取
公共件，`method_calls` 委托复用）+ `report/read_source.py`。`INSTRUCTIONS` 加「读源码优先用 read_source」。

**验收结论**：✅ 通过。`pytest -q` **220 passed**（method_calls +2：方法返回自身读写字段+已核对名+语义路由、
按行范围归属正确不串跨类方法；`tests/test_read_source.py` +7：标注已知字段名(含常量字面值)、不臆造非字段
token、行区间切片、防路径越界、缺文件、render、CLI/MCP 同口径；`test_mcp.py` TOOLS 加 `read_source`）。
**天花板仍在（诚实记录）**：`read_source` 是"引导非强制"——若模型坚持用宿主原生 reader 读源码、连
`read_source` 都不调，host-agnostic 仍拦不住；能做的是把"正确解码野生编码 + 自带字段真名 + 行号对齐 KB"
做成它用我们 reader 的硬理由。

```powershell
cosmic_kb calls "<类全限定名>" "<方法名>"   # 现额外回「本方法读写字段（已核对名）」+ 落库 + 语义路由
cosmic_kb source "cqspb/am/Foo.java"        # 读源码(正确解码)+顶部「已核对字段名」标注块；--lines 30-60 读区间
```

---

## 阶段 10 增补 · read_source 字段名三档置信消歧 ✅

**背景**：真实样本发现 `read_source` 把同一 `<isv>_xxx` key 在**多张单据**的同名候选**全平铺**返回（中文名
可能不同），段二模型只能脑补归属——这是一种严重误导。诊断：`read_source` 明明知道"在读哪个文件"，却没用
本文件的解析上下文收敛候选，直接把 `resolve_fields` 的裸全候选倒出去。

**目标**：用本文件的确定性解析结果收敛同名候选，归属能定就定、定不了就**显式标歧义**（红线 #4），绝不平铺成
"像已确定"。

**实现**：仅改 `report/read_source.py`（其余返字段名的工具 trace/bill/ask/method_calls 早已用
`FieldNames.get(key, form_key)` 按数据包来源单据取名，本无此病）。依据是本文件 `field_access.form_key`——
它存的是数据包**实际来源实体**（经 ORM load / 事件入参 / 跨实体传播解析，`analyze.py` 中 `form_key=acc.entity`；
解不出为 NULL）。`field_names` 按 key 归三档：✅`unique`（元数据唯一）/ ✅`resolved`（本文件 field_access 已把它
解析到具体单据——含 `loadSingle` 跨实体取到别张单据字段的情形——收敛 + 附依据行号 + 标明其余单据）/
⚠️`ambiguous`（多单据同名又没解析到实体 → 列候选 + 指出消歧方向「看接收变量来源 dataEntity/loadSingle/
getAllSonList」，`names` 留空、绝不替选、不默认当前单据）。返回形状变为 `{key:{tier,names,coordinates,note}|None}`；
MCP `INSTRUCTIONS`/工具说明补「ambiguous 别默认当前单据」。零 schema 改动（v9）。

**验收结论**：✅ 通过。`pytest -q` **222 passed**（test_read_source 适配新形状 + 新增 resolved/ambiguous 两档
用例：`cqkd_fee` 经本文件 loadSingle 收敛到合同单「手续费」不平铺资产卡「服务费」、附依据行号；`cqkd_rate`
无 field_access 解析 → 标歧义、列两单候选、给消歧方向）。

```powershell
cosmic_kb source "cqspb/am/Foo.java"        # field_names 三档：unique/resolved 照抄 names；⚠️ambiguous 顺调用链消歧
```

---

## 阶段 10 增补 · read_source 防 host 截断（标注在前·源码垫底·坐标瘦身）✅

**背景**：真实样本里 `read_source` 一次读较多行时被 MCP host 按字节砍尾部（`[truncated 59401 of 92169 bytes]`）。
两个问题让"被砍的恰恰是不该砍的"：① 返回 dict 顺序 `…content, field_names, note`，尾部正好是**不可替代的
`field_names`**，而 `content` 模型重读即可；② 体积大头在 `field_names` 的 `coordinates`，尤其 ambiguous 档把
跨单据所有同名候选 dict 全平铺，单个 key 就能几 KB。

**目标**：让标注存活、源码可牺牲（红线 #4 不静默丢证据）+ 显著瘦身，使整体多在 host 截断阈值内。

**实现**（仅 `report/read_source.py`）：① 返回 dict 重排，**`content` 垫到最末**、`field_names` 在前，命中 key
超 `max_keys` 的省略数从尾部 note 提到顶层 `keys_omitted`；② 新增 `_trim_coord`（每条坐标只留标名/消歧必需
字段，丢 entity_key/parent_key）+ `_cap_coords`（每个 key 候选数封顶 `_MAX_COORDS=8`，超出带 `coordinates_capped`
计数 + "全部见 resolve_fields"），三档统一应用。零 schema 改动（v9）。

**验收结论**：✅ 通过。`pytest -q` **223 passed**（新增封顶用例：跨 >8 单据同名 ambiguous key → `len(coordinates)==8`
且 `coordinates_capped>0`；并断言 `field_names` 键序在 `content` 之前、顶层有 `keys_omitted`、坐标已裁剪）。
**天花板仍在（诚实）**：host 截断阈值与序列化由宿主决定，本批只能控制"放什么、按什么顺序放"，无法保证永不截断。

---

## 阶段 10 增补 · 暴露 field_type + 取值语义，堵 getDynamicObjectCollection 误判 ✅

**背景**：真实样本里段二模型读到 `dataEntity.getDynamicObjectCollection(REFUND_CONTRACT)`（`REFUND_CONTRACT="cqkd_httz"`）
就断定是"退租合同**分录**"——错的：`cqkd_httz` 是表头**多选基础资料字段**（`field_type=MulBasedataField`），
`getDynamicObjectCollection()` 取的是选中的基础资料集合而非分录行。根因（确定性层可补的部分）：精确判别信号
`field.field_type`（XML 标签名）早在 KB 里（schema.sql/store.py），但 `resolve_fields`/`field_names` 从不返回它，
只给零区分力的 `field_kind=entity`（仅表示"有 DB 列"）。

**目标**：把"`getDynamicObjectCollection(key)` 取分录还是基础资料"的判别事实做成工具返回值里的**显式中文结论**
（延续模式 B：软约束压不住模型先验，只能焊进它必读的返回值），而非留原始枚举让它自行推断。

**实现**：① `semantic/dictionary.py` `FieldEntry` + SELECT 带上 `field_type`；② `report/resolve_fields.py` 新增
`_access_hint()`，每个字段坐标增 `field_type` + 派生中文 `access`（`MulBasedataField`→"多选基础资料字段……不是
分录行"；含 Basedata 单选→"基础资料字段"；标量字段→None 不臆造），entity 容器镜像标"分录容器"；③
`report/read_source.py` `_trim_coord` 保留 `field_type`/`access`，`_coord_line` + 顶层 note 显式点出判别；④ MCP
`INSTRUCTIONS` + read_source/resolve_fields 工具说明同步"照 field_type/access，别凭 API 名猜分录"。零 schema 改动（v9）。
**已知边界（诚实）**：读取窗口不含常量字面定义行（`REFUND_CONTRACT="cqkd_httz"`）时 token 扫不到 key、无从标注
——属"读窗太窄"，模型应放宽 `--lines`，本批不做常量→key 解析。

**验收结论**：✅ 通过。`pytest -q` **225 passed**（resolve_fields 新增 access 分档用例：多选/单选基础资料/标量
None/容器；read_source 新增 `cqkd_httz` 坐标带 `field_type=MulBasedataField` + access"不是分录"，note 与 render 均现）。

```powershell
cosmic_kb resolve "cqkd_httz"          # 字段坐标含 field_type + access：多选基础资料字段（getDynamicObjectCollection 取基础资料集合，非分录）
cosmic_kb source "cqspb/am/Foo.java"   # field_names 坐标带 field_type/access；note 点出 getDynamicObjectCollection 判别
```

---

## 信任优先 · form_key 解析率提升（绑定回落 + 泛型集合建模）✅

**背景**：`read_source` 同名跨单据消歧靠 `field_access.form_key`（数据包来源实体）收敛——form_key=None 时只能平铺
候选、诱导段二模型脑补归属。真实库（D:\kingdee\asset_management_sys）11644/19317（60.3%）的 field_access 写入
form_key=None。按 evidence 分桶定位瓶颈：① 局部 DynamicObject 变量来源未识别（~3081）；② DynamicObject 入参调用方
未传播（~2171）；③ ORM 实体实参为运行时变量（~1082，诚实未知）；④ 基础资料引用包（~918，连带）。挖到两个**可救**
根因：(A) 已绑定插件里**未被事件 BFS 覆盖**的 helper 在第②轮孤立补全时丢了绑定单据；(B) **泛型集合
`List/Set/Collection<DynamicObject>` 完全没建模**——项目里传查询结果最常用的形态，三种已建模形参（DynamicObject /
DynamicObject[] / DynamicObjectCollection）都不是，整条读写链来源 None（典型：`assignmentBill(List<DynamicObject> list)`
里 for-each 读字段，list 由 `list.add(loadSingle(id,"cqkd_ht"))` 累积）。

**目标**：在不臆造的前提下（红线 #4）补这两条断链，让更多写入定位到具体来源单据，把 `read_source` 的 ambiguous
降级为 resolved/unique。

**实现**：① **绑定回落**——`analyze.py` 建 `bound_entity`（类→其绑定单据集），第②轮 `_analyze_standalone` 对
**唯一绑定**的插件类用该单据作 `default_entity`/返回解析器基准（绑多张单据则歧义、留 None）。② **泛型集合建模**——
`ast_index.py` 加 `dynamicobject_collection_params/vars` + `_is_do_collection_type`（读原始类型文本，`<DynamicObject>`
精确匹配，保守不误伤 Map/DynamicObjectCollection）；`field_access.py` `_Env` 加 `do_coll_vars`，`_build_contexts` 加
「空集合 + `.add(已知实体包)`/`.addAll` 累积推断元素来源实体」轻量数据流（含内联 `add(loadSingle(...,"实体"))`），
多来源/解不出留 None；`analyze.py` `_coll_params` 并入 List<DO> 形参，统一走「实参↔形参」坐标传播，三处 `_Env`
注入 `do_coll_vars`。零 schema 改动（v9）。**诚实边界**：ORM 动态实体（实体名是运行时变量）、无调用方上下文的孤立
service 仍 None——真未知，不碰。

**验收结论**：✅ 通过。`pytest -q` **227 passed**（新增 `test_list_dynamicobject_param_source_propagation`：
List<DynamicObject> 形参 + `.add(loadSingle(...,"实体"))` → helper for-each 读字段来源得出；既有 `test_bound_plugin_
helper_uses_binding_entity` 覆盖绑定回落）。真实库前后对比（按稳定键 diff，严格只填空 **0 改写**）：form_key NULL
**11644（60.3%）→ 10880（56.1%）**，解析率 39.7%→**43.9%（+4.2pp）**，累计救回 764 行（绑定回落 ~228 + 集合建模 ~536）。
填空 99%+ 落在确认的元数据实体（cqkd_ht 442…）；案例 `HtRefundFormPlugin.assignmentBill` 46 个 `do.get*` 读
`None → cqkd_ht`（正确）。

```powershell
cosmic_kb build "<源码根>" "<dym|zip>"   # 重建 KB 生效（form_key 解析率提升后 trace/source 来源更准）
cosmic_kb source "<源文件>"              # 同名跨单据消歧：更多字段从 ⚠️ambiguous 升到 ✅resolved
```

---

## 信任优先 · form_key 解析率提升（字段key反查回填三层 + addNew 习语，schema v10）✅

**背景**：`read_source` 同名跨单据消歧靠 `field_access.form_key`（数据包来源实体）收敛，None 越多越只能平铺候选、
诱导段二模型脑补归属。上一轮「绑定回落 + 泛型集合建模」后真实库仍 56.1% 写入 None。`docs/form_key解析待办.md` 做
数据驱动成因分析，挖出两条**确定性可救、零臆造**的杠杆。

**目标**：在不臆造前提下（红线 #4）进一步压低 form_key NULL，且把元数据反推来源**显式区分于数据流证明**。

**实现**：① **待办一·字段 key 反查元数据回填**（`analyze.py` `_field_form_index` + `_backfill_form_key`，`_dedup` 前
对 form_key=None 且 field_key 非空的行做三层逐级塌缩）——物理硬约束「一个 DynamicObject 不可能 `.set("cqkd_xxx")`
除非其实体声明了该 key」，对返回值/Map/helper/new/stream 等一切容器断链免疫：(①唯一反查) 字段 key 在元数据只归一单
→ 直接定 form_key + level/entry_key（高置信 0.9）；(②绑定收敛) 多候选时与写它的 access_class / 入口插件绑定单据
取交，唯一→定它（0.7）；(③同对象共现交集) 同接收者变量连读写多字段 → 各字段候选 form 集合取交集，唯一→定它（0.7，
为此 `field_access.py` 给 do.* 路径记录接收者变量名 `receiver_var`）。② **待办二习语**：`field_access.py` 加
`new DynamicObject(coll.getDynamicObjectType())` 识别，新行继承集合分录坐标（与已覆盖的 `addNew()` 同理）。
新增 schema v10 列 `form_key_source`（data_flow / metadata_unique·binding·cooccur / NULL），配套 evidence 备注 +
降置信；`read_source` 据此对 metadata_* 来源诚实标注「依据是字段归属元数据反查、非数据流行号」，不冒充数据流。

**验收结论**：✅ 通过。`pytest -q` **240 passed**（新增 `tests/test_form_key_backfill.py` 12 例：三层收敛 + 留 None
诚实边界 + `_field_form_index` + 待办二/反查回填端到端经 store 落库；`tests/test_read_source.py` 加 metadata 反推诚实
note 例）。真实库（D:\kingdee\asset_management_sys，schema v9→v10）前后对比，**严格只填 None 行、0 改写既有
data_flow**：form_key NULL **all 56.1%(10880)→34.6%(6713)**、**write 52.1%(3589)→27.4%(1889)**、read 58.3%→38.6%；
回填 4153 行（metadata_unique 2167 + metadata_binding 1234 + metadata_cooccur 752），余为待办二习语转 data_flow。

```powershell
cosmic_kb build "<源码根>" "<dym|zip>"   # 重建 KB（schema v10）；form_key 解析率提升后 source/trace 来源更准
cosmic_kb source "<源文件>"              # 同名跨单据更多从 ⚠️ambiguous 升 ✅resolved；metadata 反推依据诚实标注
```

> 后续保留（`docs/form_key解析待办.md`）：② 孤立方法反向调用图回填（~2545 行，跨方法需防环/防爆，复杂度更高）；
> ③ ORM 动态实体名 + ④ 基础资料引用包属诚实未知（留 None 正确，不做）。

---

## 信任优先 · trace 防 MCP 截断（写/读拆分 + 按类合并 + 字节 governor）✅

**背景**：段二大模型经 MCP 调 `trace` 时返回被宿主从中间截断——真实样本 67,879 字节被切到只剩
32,768（**32KB 是 MCP host 写死的硬上限**，改不了）。先做过一轮"删 evidence 死重列 + 白名单投影 +
各数组 cap"（`tests/test_field_trace_bounded.py`、readers 折叠成「该读方法」清单、顶层扁平 writers/
readers 删除），仍 67KB。根因：**撑爆的是数组条数本身无界**——坐标组数 + `unlocated/possible/coarse/
dynamic` 等独立数组都无界，per-section 行级 cap 管不住。

**目标**：给「经 MCP 的 trace 永不被 host 截断」的硬保证，且不丢任何真实计数（红线 #4）；只改 MCP
路径，CLI 文本 / 本地 Web（HTTP 无 32KB 限制）拿完整结果不动。

**实现**：MCP 走新的紧凑投影 `field_trace.trace_compact`——① **写/读拆分**：`access` 参数（`'write'`/
`'read'`/默认）把"谁写了它（是否落库）"与"谁读了它"两个排障问题拆开，每次只返一半；② **按类合并**：
把散落的行/方法按 `access_class or plugin_fqn` 塌缩成**有界的类节点**（`_merge_writers_by_class` 类→
sites、`_merge_readers_by_class` 类→方法、`_readers_overview` 仅类+计数），每行重复的插件常量
（plugin_type/plugin_form_label/semantics_topic）提到类节点只存一份（信息无损）；③ **cap + 字节
governor**：cap 类节点数，再按 `json.dumps` 字节预算（28000）逐级收紧 cap 重建直至 ≤ budget，真实总数
恒在 `summary`、截掉的量在各节点 `capped`/`sites_capped`/`methods_capped`。复用红线 #6：抽出
`_collect_materials` 共享取数/enrich/分桶，富 `field_trace()` 输出**逐字节不变**（现有测试不改即过）。
`mcp/server.py`：`tool_trace` 加 `access` 走 compact、`tool_ask` 字段意图的 evidence 同样换 compact
（堵 `ask` 这条同样会截断的路径，复用 rq 不动 builder/CLI）、`INSTRUCTIONS` 改写写读拆分纪律。

**验收结论**：✅ 通过。`pytest -q` **253 passed**（新增 `tests/test_trace_compact.py` 13 例：access 过滤
/按类合并去重/cap 真实总数/**字节 governor ≤30000 硬保证**；`tests/test_field_trace_bounded.py` 上一轮
瘦身回归；`test_mcp.py` 两例对齐新口径）。真实库（D:\kingdee\asset_management_sys）`cqkd_ht.cqkd_zdgl.
cqkd_qs`：富 dict 51456 字节（会被截）→ compact **默认 22761 / write 20217 / read 23182** 字节，全部
< 32KB 完整返回。（注：宿主 MCP server 常驻，改源码后需**重连/重启 MCP** 才加载新代码。）

```powershell
# MCP 工具：trace 默认=写入明细+读取按类概览；access='read' 看读取类→方法明细；access='write' 只写入
# 真实总数恒在 summary，被 cap 截断量在节点 capped；按 form/entry/level 收窄看更多
```

---

## 信任优先 · 提高字段扫描率（模型形参识别 + 内联集合链，1+2+3）✅

**背景**：比 form_key=None 更糟的一类是**字段写入根本没被扫出**——`field_access` 记录都不产生，查询时
彻底隐形。对真实库剩余漏扫归类出三条边界清晰、高收益、低误报的确定性缺口，全在 `field_access.py` 的
轻量数据流里：① helper `void calc(IDataModel model){ model.setValue("x", v); }` 的写入被整条丢弃
（`_is_model_receiver` 只认 `getModel()` 结尾或已知 `model_vars`，方法**形参**永不入集）；② 内联
`X.getDynamicObjectCollection("k").addNew()` 赋给 DynamicObject 局部时，`_GET_COLL_ARG_RE` 先命中把新行
**误当成集合** → 后续 `row.set(...)` 整片判不出；③ 内联 `X.getDynamicObjectCollection("k").forEach(o->o.set(..))`
/ `.stream()` lambda 的行变量拿不到元素上下文（`_lambda_collection_var` 取到 base `X` 非集合变量）。

**目标**：把这三类写入正确记录成 `field_access`，坐标尽量补全；来源解不出仍由现有「字段key反查回填」
兜底，**零 schema 改动（v10 不变）**、零臆造（红线 #4）、低误报。④反向调用图、⑤QFilter 查询访问表留后续。

**实现**：① **模型形参识别**——`analyze._model_params` 按类型白名单 `_MODEL_TYPES` 抽形参名注入
`_Env.model_params`，`_build_contexts` 播种 `model_vars`；模型 API 来源改走 `_model_entity`（跨类 service 收
`getModel()/getView()` 定到绑定单据，插件自身回落 `default_entity` 不变；仅类型入集、不靠变量名猜=低误报）。
② **内联 addNew 链**——新增共享闭包 `_inline_coll_elem`（`OWNER.getDynamicObjectCollection("KEY")` → 元素行
坐标，OWNER 解不出则 entity=None），并在 `m_coll` 判定**之前**加内联守卫（仿 stream 前置）。③ **内联 lambda
链**——`_lambda_recv` 回传接收者原文，lambda 绑定处复用 `_inline_coll_elem` 兜底。`addNew`/`new DynamicObject`
的**变量形式**早由上一轮覆盖，本轮专补**内联链**。

**验收结论**：✅ 通过。`pytest -q` **264 passed**（新增 `tests/test_field_scan_inline.py` 11 例：C1 模型形参
读写/跨类绑定来源/无绑定回落/IFormView.getModel() 链/**非模型形参不入账误报护栏**；C2 内联 addNew 坐标/owner
未解 entity=None/变量形式回归；C3 内联 forEach/stream lambda 坐标/变量形式回归）。真实库
（D:\kingdee\asset_management_sys，同 `samples/appzip`+`samples/trans` 元数据）field_access **19399→20104
（+705：写 +402 / 读 +303，此前完全不可见）**，其中 via model.* +429（C1）、内联 do.* +276（C2/C3）；form_key
NULL **率不升反降 34.6%→34.4%、写 27.4%→26.7%**（新行多数当场定到来源，不进未知堆，证无臆造爆量）。

```powershell
cosmic_kb build "<项目源码根>" "<dym|zip|目录>"   # 重建 KB 后字段扫描率提升即生效
```

---

## 信任优先 · form_key 解析率提升（孤立方法反向调用图回填）✅

**背景**：`form_key=None` 未定位的最大单一杠杆是「孤立方法 DynamicObject 入参」——`docs/数据包来源与
form_key解析合并.md` §2/§5 实测占未定位 25.0%（1,679 条）。成因：正向分析只从「绑定插件事件 / 未绑定苍穹
插件根方法」出发跨类 BFS，沿「实参↔形参」传播来源；形如 `void fill(DynamicObject o){ o.set("cqkd_x", …) }`
的 helper 若**没被任何事件 BFS 覆盖**，只走全量孤立补全，DO 形参不知来源 → 写入整片 form_key=None。

**目标**：在不臆造前提下（红线 #4）补这条断链——helper 的项目内调用链可解析、且调用方实参来源能安全
收敛到同一来源时，反向把来源沿「实参↔形参」传播进来回填 form_key；递归/冲突/源未知留 None。零 schema
改动（v10）。

**实现**：集中在 `cosmic_kb/java/analyze.py`，`_backfill_form_key`（元数据反推）**之前**插入新轮
`_backfill_reverse_calls`（反向调用图给的是真实数据流来源，强度高于字段归属反推，先定）。
① `_build_reverse_calls` 遍历全项目逐重载方法体，用现成 `_resolve_call` 建「目标(类,方法)→调用点」反向索引
（自调用天然不入索引，`new Helper().m()` 显式构造接收者可解析）；② 对 form_key=None 的 standalone 行按
(access_class, event_method=物理方法) 分组，限域到这些目标的反向可达子图；③ `_propagate_reverse_props`
固定点逐跳传播：用调用方当前 prop 重跑 `analyze_method`，再复用 `_callee_prop` 算目标「实参↔形参」上下文，
只有全部调用点都推出同一个非空来源才采纳；④ 按 (line, field_key, access) 把重解析出 `entity≠None` 的项
回填到原 None 行，标 `form_key_source=reverse_callgraph`。单跳唯一调用方 confidence 最高 0.85；链式或
多调用方一致最高 0.80。只动 None 行、绝不改写已定位行。复用 `_RetResolver`/`_callee_prop`/
`analyze_method`/`fa._Env`，不改 field_access/store schema。
`read_source.py` 按 `metadata` 前缀判来源依据，`reverse_callgraph` 自动归「数据流类」，无需改动。

**验收结论**：✅ 通过，但收益未达 500+ 目标。`tests/test_reverse_callgraph.py` 扩到 11 例——唯一调用方 ORM
来源回填（写+读，source=reverse_callgraph、confidence≤0.85）/ 三跳链式 helper / 多调用方来源一致可回填 /
多调用方来源冲突留 None / 多调用方含未知来源留 None / `new Helper().fill(...)` 接收者解析 / 零调用方留 None /
调用方实参源未知留 None / 递归不算调用方 / 已定位行不被覆盖 / **端到端**（字段 key 元数据**多单据歧义**·
metadata 救不了，唯反向调用图能定到 cqkd_bill，经 store 落库 source=reverse_callgraph）。Codex 侧复验：
`pytest tests/test_reverse_callgraph.py` 11 passed；相关套件
`test_form_key_backfill`+`test_field_scan_inline`+`test_java_field` 64 passed；全量 `pytest -q` **278 passed**。
真实库重建复算：
`cosmic_kb build "D:\kingdee\asset_management_sys" "D:\codex\cqkd_ai\samples\appzip" "D:\codex\cqkd_ai\samples\trans"`
成功生成 `D:\kingdee\asset_management_sys\cosmic_kb.db`，`field_access=20259`；
`reverse_callgraph=218`（读 148 / 写 70，覆盖 12 个方法），总 `form_key NULL=6903/20259=34.07%`，
写访问 NULL `1930/7336=26.31%`。相对单跳版 202 仅新增 16 条，未达 500+ 目标；结论是剩余缺口不适合继续
放宽反向调用图红线，下一步应转向实例字段级数据流、非标准来源/容器来源、动态 evidence 分桶专项。
`cosmic_kb coverage --db D:\kingdee\asset_management_sys\cosmic_kb.db` 通过，业务字段覆盖 38%（2373/6209）、
字段标识解析可信 94%、来源定位 66%。限域优化后真实构建耗时约 36.4 秒。

```powershell
cosmic_kb build "<项目源码根>" "<dym|zip|目录>"   # 重建 KB 后反向回填生效；coverage 复算 form_key NULL 率
```

---

## 阶段 10 增补 · MCP 工具面精简（10→7，审计工具下沉 CLI-only）✅

**背景**：段二大模型经 MCP 调 `cosmic_kb`，当前暴露 **10 个工具**（`cosmic_kb/mcp/server.py` 的 `TOOLS`）。
真实使用中「工具太多、职责重叠、模型选不准」：① `coverage`+`scan_compare` 都是「扫描可信度/覆盖率」
报告、模型难判调哪个，且本质是**人类验收/审计**视角产物、非段二排障主路径；② `dynamic_writes` 是
**全项目盲点审计**工具，模型在「谁写了字段 X」时易误调它、绕开 `trace`；③ `ask` 与
`trace/bill/method_calls` 入口重叠，模型在已知精确标识时仍可能偷懒调 `ask`，结果不如专用工具稳。

**目标**：把 MCP 暴露面收敛到 **7 个排障核心工具**，让模型选工具清爽、不误用审计工具。**仅改 MCP
工具面**——CLI 16 个命令、KB schema（v10）、取证函数（`report/*`）全部不动（红线 #6：KB 是契约）。
审计/质量报告（coverage/scan-compare/dynwrites）继续由 **CLI 服务人类**，只是不再对 LLM 暴露。

**实现**：`cosmic_kb/mcp/server.py`——① 删除三个审计工具纯逻辑函数 `tool_coverage`/`tool_scan_compare`/
`tool_dynamic_writes`（CLI 走 `report.*` 直连、不经这些 wrapper，删后无悬挂引用）；② `TOOLS` 注册表
收敛为 7 项 `ask, trace, bill, method_calls, resolve_fields, read_source, cosmic_semantics`；③ 收紧
`INSTRUCTIONS`：路由首句给 `ask` 补「知道精确标识就直接用 trace/bill/method_calls，ask 是判不准意图
时兜底」、【动态写入】段删掉指向已不可达 `dynamic_writes` 的过滤切片句（**保留** trace 返回值里的
`dynamic_writers` 该读方法清单导航）；④ 收紧 docstring（FastMCP 用 docstring 生成工具 schema，是模型
选工具主依据）：`ask` 标「兜底路由」、`cosmic_semantics` 把空参列清单的发现路径提前显眼、
`resolve_fields`↔`read_source` 各补一句边界（已在读源码用 read_source、它已自带 field_names；手上只有
key 不读源码才用 resolve_fields）、`method_calls` 强化「只导航不解释」。CLI / web / `.mcp.json` 不动。

**验收结论**：✅ 通过（Claude 开发 + 写用例；Codex 执行测试 + 更新验收 + 提交）。静态自检
`python -c "from cosmic_kb.mcp import server as s; print(sorted(s.TOOLS)); ..."` 打印 7 个名字：
`ask/bill/cosmic_semantics/method_calls/read_source/resolve_fields/trace`，且
coverage/scan_compare/dynamic_writes 既不在 `TOOLS`，也没有残留 `tool_*` 函数；未装 `[mcp]` 仍可 import。
`tests/test_mcp.py`：
`test_tools_registry_matches` 期望集合改为 7 名，新增 `test_audit_tools_not_exposed_to_mcp`
（断言三审计工具不在 `TOOLS`、`tool_*` 函数也已删，防回归混入）；删除
`test_tool_coverage_and_scan_compare`（其纯逻辑仍由 `report.*` + `test_coverage.py`/`test_scan_compare.py`
专属套件覆盖，无测试缺口）。Codex 复验：MCP + 反向调用图 + 审计相关套件
`pytest tests/test_mcp.py tests/test_reverse_callgraph.py tests/test_coverage.py tests/test_scan_compare.py tests/test_dynamic_writes.py -q`
为 **42 passed**；全量 `pytest -q` 为 **278 passed**。`python -m cosmic_kb.cli.main --help`
确认 CLI `coverage`/`scan-compare`/`dynwrites` 仍可用（审计能力只是离开 MCP、未消失）。注：宿主 MCP server
常驻，改后需**重连/重启 MCP** 才生效，host 重新拉清单应只见 7 个工具。

```powershell
python -c "from cosmic_kb.mcp import server as s; print(sorted(s.TOOLS))"  # 应打印 7 个、不含审计工具
cosmic_kb coverage        # 审计能力仍在 CLI（仅离开 MCP）
cosmic_kb scan-compare
cosmic_kb dynwrites
```

---

## 阶段 11 · 增量重扫 + GitNexus 增强 ⬜

计划交付：文件指纹增量重扫；接 GitNexus 补调用链/影响范围。

---

_对应里程碑：M1 信任地基(1–4) · 本地 Web(4.5) · M2 行为分析(5–8) · M3 AI 问答(9–10) · M4 增强(11)。
当前进度：阶段 0–10 ✅（均已验收通过，含字段级排障引擎、可信度报告、ask、MCP；方法 `calls` 调用导航待人工测试）；
分发地基（自包含包重构 + MCP 语义下沉）✅ 已验收；unknown 字段诚实分类 + 动态写入交段二 ✅ 已验收；
字段名核对 resolve_fields ✅ 已验收；模式 B 语义增强（核对结果焊进工具返回值）✅ 已验收；method_calls 带字段 +
模式 A read_source（堵两条读源码猜字段名路径）✅ 已验收；read_source 字段名三档置信消歧 ✅ 已验收；
read_source 防 host 截断（标注在前·坐标瘦身）✅ 已验收；暴露 field_type + 取值语义堵 getDynamicObjectCollection
误判 ✅ 已验收；form_key 解析率提升（绑定回落 + 泛型集合建模，60.3%→56.1% NULL）✅ 已验收；
form_key 解析率再提升（字段key反查回填三层 + addNew 习语，schema v10，56.1%→34.6% NULL）✅ 已验收；
trace 防 MCP 截断（写/读拆分 + 按类合并 + 字节 governor，trace_compact）✅ 已验收；
提高字段扫描率（模型形参识别 + 内联集合链 1+2+3，field_access +705 行、NULL 率不升反降）✅ 已验收；
form_key 解析率提升（孤立方法反向调用图固定点回填，reverse_callgraph）✅ 已验收（Codex 侧全量 `pytest -q` 278 passed；真实库回填 218 条，NULL 率 34.07%，未达 500+ 目标，后续转实例字段/非标准来源专项）；
MCP 工具面精简（10→7，审计工具 coverage/scan_compare/dynamic_writes 下沉 CLI-only）✅ 已验收；
阶段 8 ⬜ 搁置（折进阶段 9 按需）；阶段 11 ⬜ 待开发。_

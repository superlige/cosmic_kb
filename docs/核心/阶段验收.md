# 苍穹历史项目本地理解工具 — 阶段验收台账（表格版）

> 本台账**只记当前状态**，不记实施过程/返工细节——那些留在 git log、测试文件与代码注释里，
> 需要深挖某条改动的来龙去脉时查 git log 即可。每个模块一张表：**功能点 | 增强内容 | 状态**。
> 曾经上线后又被整体删除/替代的功能，只留一行「🗑 已退役」+ 一句话去向，不保留其历史迭代过程。
>
> 图例：✅ 已验收 ｜ ⬜ 待 codex 跑 `pytest -q` 确认 ｜ 🗑 已退役/被替代
>
> 当前状态：schema **v20**；MCP 工具面 5 个（`trace`/`bill`/`resolve_fields`/`callers`/
> `cosmic_semantics`，`ask` 已退役）；本地全量 `pytest -q` 结果以 codex 最近一次执行为准，本表 ⬜ 条目
> 均为尚未经 codex 全量确认的改动。产品方向 2026-06-17 起：从「项目普查」转向「字段级排障导航」。
> 专题深挖见：`docs/设计方案/数据包来源与form_key解析合并.md`（form_key 识别率）、
> `docs/参考手册/read_source字段名解析逻辑.md`（已废弃功能的历史设计记录）。

## 模块总览

| 模块 | 目录 | 一句话 | 状态 |
|---|---|---|---|
| 脚手架 | — | 项目骨架 + CLI 入口 | ✅ |
| 摄取 ingest | `ingest/` | 递归读野生 Java 源码（编码探测+容错解析） | ✅ |
| 元数据 metadata + dbmeta | `metadata/` `dbmeta/` | dym/zip/底层库 → 统一 `MetaModel`，含原厂/自家元数据同步 | ✅ |
| 桥接 bridge | `bridge/` | 元数据 `<ClassName>` ↔ 源码类，五态分类 | ✅ |
| 图谱 graph | `graph/` | SQLite+FTS5 知识库存储 + 项目地图 | ✅ |
| Web 展示 | `web/` | 本机浏览器排障界面 | ✅ |
| Java 语义引擎 | `java/` | 字段级读写/落库/来源静态分析（核心能力） | ✅ 持续加固 |
| 排障取证接口 report | `report/` | trace/bill/resolve_fields/callers | ✅ 持续加固 |
| 可信度审计 | `report/` | coverage / scan-compare（CLI-only） | ✅ |
| 语义解析 semantic | `semantic/` | 词典底座（`resolve_fields` 用）；`ask` 整体退役 | 🗑 部分退役 |
| MCP 封装 | `mcp/` | 段二大模型接入（5 工具） | ✅ |
| 编译期符号解析 symbols | `java/symbols/` + `tools/symsolver/` | 类路径发现 + JVM 微工具，跨类调用精确绑定（阶段 12） | ✅ 12.1–12.3 完成 |
| 增量重扫 / GitNexus | — | 阶段 11 规划 | ⬜ 待开发 |

---

## 一、脚手架

| 功能点 | 增强内容 | 状态 |
|---|---|---|
| 阶段 0 · 脚手架 | `cosmic_kb/` 两段式子包骨架 + CLI 入口 + 可编辑安装 + 可选依赖分组 | ✅ |

---

## 二、摄取层 ingest

| 功能点 | 增强内容 | 状态 |
|---|---|---|
| 阶段 1 · 源码摄取 + 可信度报告 | 编码自动探测 + tree-sitter 容错解析；真实项目 1345 文件 100% 读取/解析、0 乱码 | ✅ |

---

## 三、元数据层 metadata + dbmeta

| 功能点 | 增强内容 | 状态 |
|---|---|---|
| 阶段 2 · 元数据解析 | 三类 dym（单据/基础资料/动态表单）→ 统一 `MetaModel`；hex oid 回填；完全沿用模板而未在业务 XML 重复写出的预制操作（如 `refresh`）补齐；整包双层 zip；多包汇总 | ✅ |
| `.cr` 转换规则解析 | 转换规则解析入图谱 `convert_rule` 表 + `converts_to` 边 | ✅ |
| 底层库元数据源（只读直连） | 新增 `dbmeta/` 只读连原厂库补全元数据；SQL 白名单 + 只读会话 + 永不 commit 三重防线 | ✅ |
| 原厂元数据并入 KB | 选择性摄取原厂**字段+操作**（不要插件），schema v12 加 `is_extension`/`extends`，扩展 key 查询自动重定向 | ✅ |
| 原厂插件 `enabled=False` 例外保留 | 原丢弃规则误伤"确认禁用"信号：`strip_vendor_plugins` 无条件清空原厂插件，若同类名在别处（继承拷贝等）还有未带 `Enabled` 标签的条目，`resolve`/`bill` 会把已确认禁用的插件误报成 `enabled=null`（未知）；真实翻车样本 `kd.cf.lgc.ht.opplugin.AdjustAmountOpplugin`（账单调整单据，原厂层已禁用）。改为仅保留原厂插件里 `enabled is False` 的条目（无源码但"确认不执行"仍是有效信号），`enabled=True/None` 继续按拍板丢弃；`merge_vendor_extension` 相应把这部分保留插件并入最终 `plugins` | ✅ |
| `--discover` 真实项目降噪 | 已知 key 扩展到 field 表 + 排除 `fk_`/`tk_` 物理库前缀；真实项目候选 4584→164 | ✅ |
| 三类确定性信号自动摄取 | `from_extensions`/`from_orm_calls`/`from_operation_calls` 三信号取代形状过滤；`--db-config` 自动并入，免手敲 `--vendor` | ✅ |
| 修复扩展候选 fnumber 截断 | 改用本地扩展自身 fnumber 经 `fmasterid` 自关联查母体，不再靠猜的候选直查 | ✅ |
| 批量查库替代逐候选循环 | 候选查询固定 4 条批量 SQL，不随候选数线性增长 | ✅ |
| 修复同 key 重复并入 | 无扩展命中分支 append 前查重复 key，改走 merge，避免同 key 入库两份 | ✅ |
| 修复 trace 丢粗扫盲点线索 | `coarse` 不再只在 `access=read` 时才挂，三种 access 视图一致 | ✅ |
| dbmeta 增量→改回全量同步 | 自家二开元数据同步由「按水位增量」改「每次全量校验」，堵住"未变更实体被误判成陌生原厂实体"的坑 | ✅ |
| 修复同一原厂实体重复合并 | 截断扩展候选 key 与源码完整字面量候选解出同一母体时，`produced_keys` 去重只产出一份，堵住分录/字段重复入库 | ✅ |
| 自动识别 ISV 通用排除名单扩容 | `ysq` 经用户实测确认（2026-07-13）也是苍穹平台原厂自带 ISV（非二开），并入 `_HARDCODED_EXCLUDED_ISV`（原仅 `kingdee`），`--db-config` 自动发现候选时一并剔除，减少不必要的 `--isv` 消歧 | ✅ |

**验收命令**：`cosmic_kb build "<源码根>" "<dym|zip|目录>" --db-config cosmic_db.json [--isv <ISV>]`

---

## 四、桥接层 bridge

| 功能点 | 增强内容 | 状态 |
|---|---|---|
| 阶段 3 · 元数据↔代码桥接 | 唯一以 `<ClassName>` 全限定名为键映射；五态分类 + 孤儿收录；真实库插件命中率 91.1% | ✅ |
| 转换插件桥接 + 插件基类孤儿 | 转换插件纳入桥接；插件基类孤儿传递闭包识别 | ✅ |
| 独立校验器识别盲区修复 | `AbstractValidator` 校验器识别为一等插件种类 `validator`，79 个真实校验器不再隐身 | ✅ |
| 已绑定插件类 `plugin_base` 补全 | `resolve_plugin_classes` 传递闭包此前只在孤儿循环里消费，已绑定元数据的插件类 `source_class.plugin_base` 永远 NULL；`BridgeResult` 新增 `plugin_bases`（全量 fqn→基类，孤儿的值是其子集），`graph/store.py` 改读这份全量表；需重跑 `cosmic_kb build` 让已有 KB 补全 | ✅ |

**验收命令**：`cosmic_kb bridge "<项目源码根>" "<dym|zip|目录>"`

---

## 五、知识图谱存储 graph

| 功能点 | 增强内容 | 状态 |
|---|---|---|
| 阶段 4 · 知识图谱存储 + 项目地图 | SQLite+FTS5 幂等重建；多信号模块识别；`overview`/`project_map` | ✅ |
| `build` 全程进度显示 | 大项目建库此前长时间无输出、易被误判"卡住"（硬约束 #3 明确要求进度）。新增顶层 `cosmic_kb/progress.py`：`Progress` 协议（NULL 静默默认，MCP/bootstrap 路径零输出）+ `ConsoleProgress` 渲染器（stderr；TTY 单行 `\r` 原地刷新、非 TTY 限频 3s 换行打印）。`build` 按 `[k/N] 阶段名 ✓ 耗时` 推进全部阶段（源码扫描→符号解析→元数据解析→[二开同步→原厂补充]→桥接→元数据灌库→Java 字段级分析→灌库粗扫，可选阶段按入参增删、`_plan_build_stages` 与 `store.BUILD_STAGES` 两侧共用常量），重阶段内按子步骤打点（解析工程 Java/插件归因/孤立补全/反向调用图/粗扫… `done/total (pct%)`）；符号解析接通 runner 早已预留的 `on_event`（JVM 逐文件事件）。真机验收后修三处：TTY 补白按**显示列宽**算（中文 2 列，按 `len()` 少补会露旧行尾巴）、子步骤切换必渲染（否则新子步骤开场 tick 被限频吞掉，屏幕定格旧 100% 像卡死）、固定点传播逐边打点（analyze 最后一段重活，真实项目可达几十秒）。测试 `tests/test_progress.py` | ✅ |

> schema 版本演进：v5（插件基类孤儿）→v7（字段级 `field_access` 坐标索引）→v8（粗扫对比）→
> v9（多轮取证增强）→v10（`form_key_source`）→v11（`null_reason`）→v12（原厂扩展 `is_extension`）→
> v13（`java_constant` 常量表）→v14（下拉选项 `field_combo_item` + 基础资料引用反查）→
> v15（`operation.has_plugin`→`has_operation_plugin` 改名，字段自解释、防与 `edge.kind=
> 'has_plugin'` 混淆；顺手给 `field(key)`/`entity(key)` 加索引，配合 `resolve_fields`/trace
> occurrences 按 key 查询）→v16（`plugin.enabled` 三态入库，`bill` 已禁用插件独立小节）→
> v17（`operation_trigger` 程序化操作触发点）→v18（`field_access.edge_source` +
> `operation_trigger.receiver_source`，符号/文本精度来源显式落库）→**v19**（全量
> `call_edge`：项目/平台/类内/method_reference/failed 调用点 + target/caller 双索引）。
> 改 schema 后需 `cosmic_kb build` 重建 KB。

---

## 六、本地 Web 展示层 web

| 功能点 | 增强内容 | 状态 |
|---|---|---|
| 阶段 4.5 · 本地 Web 展示层 | 纯表现层复用 KB，仅绑 127.0.0.1；含「扫描可信度」页签 + bill 插件车道视图 | ✅ |

**验收命令**：`cosmic_kb web --open`

---

## 七、Java 语义分析引擎 java（字段级排障核心）

> 全项目投入最大的模块。全部改动严守红线 #4——只做确定性可证的收敛，钉不出就诚实留
> `None`/`unknown`，不臆造。真实库 form_key NULL 率总趋势 **60.3% → 34.07%**（详见
> `docs/设计方案/数据包来源与form_key解析合并.md`），结论：红线内已无安全的高收益提升空间，
> 后续转向诊断透明度（`null_reason`）而非继续拉数字。

| 功能点 | 增强内容 | 状态 |
|---|---|---|
| 阶段 5+6+7 · 字段级排障引擎（旗舰） | 字段标识 → 读写插件+事件函数+是否落库+行号，按坐标(单据·层级·分录)分组；schema v7 | ✅ |
| unknown 字段诚实分类 | `key_resolution` 四码（dynamic-loop/concat/external-const/unknown）+ 折叠成「该读方法」清单交段二定性 | ✅ |
| form_key① 绑定回落 + 泛型集合建模 | NULL 率 60.3%→56.1% | ✅ |
| form_key② 字段 key 反查三层回填 + addNew 习语 | NULL 率 56.1%→34.6%；schema v10 加 `form_key_source` 区分数据流证明/元数据反推 | ✅ |
| 提高字段扫描率 | 模型形参识别 + 内联集合链，补回此前完全扫不出的写入 +705 行 | ✅ |
| form_key③ 孤立方法反向调用图回填 | 固定点传播回填 218 条；结论确立"红线内已无安全高收益空间" | ✅ |
| 注解驱动 POJO↔DynamicObject 映射识别（A档） | 消灭反射写字段完全隐形问题 | ✅ |
| null_reason 未定位成因确定性落库 | 8 个互斥成因码 + 中文标签；schema v11 | ✅ |
| 修复变量作用域冲突误判 basedata | for-each/lambda 循环变量正确覆盖旧 basedata 绑定，真实写入不再被错盖成"正确 None" | ✅ |
| basedata「写」拆出 `basedata-write-suspect` | 写基础资料不再标"正确 None"，改标"应继续追" | ✅ |
| 隐藏坑 #1 · 程序化操作触发点提取入库 | `java/op_trigger.py`：`executeOperate`（arg0=操作 key、arg1=目标单据，复用 `ConstantTable.resolve_arg` 字面量/常量解析）+ `invokeOperation`（操作 key=arg0，目标=本类唯一绑定单据推断，多绑定 ambiguous/无绑定 unknown）两种调用点 → 结构化 `OperationTriggerRow`（调用类·方法·行号·via·双解析置信度）；`analyze` 全量扫描采集（不依赖插件 BFS，定时任务/service 类也扫到）；新表 `operation_trigger`（正查/反查双索引），schema v17。只存**单跳原子事实**，多跳级联链由段二 agent 递归查 bill/trace 拼出（`execOperate` 曾误当作 `executeOperate` 同签名别名识别，2026-07-20 经用户核实苍穹平台无此方法后已从识别规则中去掉） | ✅ |

**验收命令**：`cosmic_kb trace "单据.分录.字段"` / `cosmic_kb build "<源码根>" "<dym|zip|目录>"`

---

## 八、排障取证接口 report（trace / bill / resolve_fields）

> 贯穿主线：① 堵"大模型按命名惯例猜字段名/臆断语义"；② 防 MCP host 32KB 硬截断——被截内容
> 对消费方等于丢失（红线 #4），逐工具补齐折叠/字节 governor/游标分页。`read_source` 与
> `method_calls` 两个工具**已上线又被完整退役**，历史迭代不再展开，只保留退役原因。

| 功能点 | 增强内容 | 状态 |
|---|---|---|
| `resolve_fields` 字段名核对 | O(1) 标识→真实中文名+坐标，覆盖 field/entity/form 三表 | ✅ |
| 模式 B 语义增强（焊进返回值） | `field_name`/`semantics_topic` 焊进 trace/bill 返回值，堵"绕工具猜字段名" | ✅ |
| 暴露 `field_type` + 取值语义 | 堵 `getDynamicObjectCollection` 被误判成分录 | ✅ |
| trace 防 MCP 32KB 截断 | `trace_compact`：写/读拆分 + 按类合并 + 字节 governor + 游标分页，真实总数恒在 summary | ✅ |
| bill 防 MCP 截断 | `bill_compact` 同款治理，真实库 344 单据 overview 恒 <32KB | ✅ |
| trace「未定位单据」工作单化 | 按 (入口类·事件方法) 去重成工作单 + 插件注册单据线索 | ✅ |
| bill 插件清单车道分流（轴A） | 按 `plugin_type` 分场景车道 + 语义路由；真实样本 25 插件→3 车道 | ✅ |
| bill 插件 `enabled` 三态入库 + 已禁用插件独立小节 | `MetaPlugin.enabled` 此前建库时被丢弃（`store.py` 两处 `plugins.append` 未取该字段），车道/文本渲染因此无法区分"当前启用"与"已禁用（Enabled=false）"插件，易被大模型误判为仍生效；`plugin` 表补 `enabled` 三态列（schema v16），`bill_view` 把 `enabled=0` 的插件从场景车道整体剔除、单独归入 `disabled_plugins`（车道计数因此只反映实际生效插件数），CLI/MCP/紧凑投影同步；unknown（NULL）不臆断为禁用，仍留在车道里 | ✅ |
| trace/bill 顶层翻页门 `pagination` | 散落各段的 `next_cursor` + 一句 note 提醒不够——大模型看到某段 `capped=0` 就当"数据齐了"下结论，翻页规则形同虚设；改成返回体第一个 key 即 `{complete, pending}`，`pending` 列出全部未取全段的 `next_cursor`，一眼可判、不必逐段阅读理解 | ✅ |
| bill 紧凑投影加 `profile` 分档 | 真实调用反馈"解读某方法/单据"场景默认返回偏冗余；`bill_compact`/`tool_bill` 新增 `profile` 参数：默认 `"overview"`（单据概览+插件绑定，不含 `fields`/`entity_touch`）、`"full"`（补回这两段，与旧版一致）；两档均可 `cursor="fields@0"`/`"entity_touch@0"` 单独翻页取回，不静默丢证据 | ✅ |
| `event_topic()` Tier 0 权威校验 | 原先 `beforeDoOperation`/`afterDoOperation`/`itemClick` 硬编码进方法名强信号表，与 `event_extractor` 的种类事件表矛盾（这三个方法只在 `_FORM`/`_LIST` 里，从未在 `_OP`），导致表单插件的界面回调被误标成操作事务事件；新增 Tier 0：`plugin_type` 落在 `event_extractor.EVENT_TABLE` 时用 `classify_method()` 权威校验，命中才采信，三个歧义方法名从旧表移除 | ✅ |
| `dataEntity.set(entry_key, collection)` 整体赋值误判表头 | `_resolve_do_ctx`（含其 `doc_ctx` 预置的表头包变量）原先只看接收者变量坐标就判 header，不管 key 是不是分录/子分录整体实体 key；`_known_entities()` 扩展成 `key→level` 字典，`_resolve_do_ctx` 统一在判到 header 后纠偏：key 精确命中 entry/subentry 就改判该层级（entry_key 自引用，来源实体沿用原变量），层级冲突/查不到维持 header 不猜错 | ✅ |
| trace `occurrences` 补 `entity` 表查询 | 分录/子分录容器 key（如 `cqkd_zdgl`）只登记在 `entity` 表、从不在 `field` 表，原查询恒为空——查容器整体赋值的定义坐标时"写入记录判 entry"与"定义坐标查不到"自相矛盾；`occurrences` 追加一次 `entity` 表按 key 查询，`source` 字段区分 `field`/`entity_container` 两类命中 | ✅ |
| `resolve_fields` 新增 `mismatched_kind` 纠错 | 指定 `kind` 查不到时（如 `cqkd_entry` 传 `kind="field"`，实为分录容器），反查 `kind=None` 看是否命中其它种类，命中则给顶层 `mismatched_kind`（`requested_kind`/`actual_kinds`/`candidates`，带限定符时加 `qualifier_matches`）；与 `mismatched_form` 互斥触发（先证种类对不对，种类对了才查单据对不对） | ✅ |
| `parse_locator` 两段式硬猜 `header` 挤爆容器整体赋值 | 真实使用反馈：`trace("单据.字段")` 查分录整体赋值容器 key（如 `cqkd_zdgl`，只登记在 `entity` 表、真实层级 entry/subentry）仍落进"可能命中"桶，`filter` 回显 `level:"header"` 令人以为该层级确实查无结果——根因是两段式硬猜 `level="header"`，`_is_exact` 按 header 硬匹配把真实层级是 entry 的写入行挤出精确桶；两段式改为只锁单据、`level` 留 `None`（`_is_exact` 只按单据过滤，该单据内该 key 有几层就分几组），`resolve_fields` 的 `_split_qualified` 本就没有这个硬猜、无需改 | ✅ |
| resolve_fields 补齐表单中文名 | 新增查 `form` 表；docstring 明确排除"调用预算紧张跳过核实"这条合理化路径 | ✅ |
| resolve_fields 补齐复合限定符 | 支持 `分录.字段`/`单据.分录.字段`，双维度 `mismatched_form` 诚实提示 | ✅ |
| 成因码中文标签焊进返回值 | `null_reason`/`key_resolution`/`field_kind` 不再是裸 code，`trace`/`resolve_fields` 返回值旁附中文标签 | ✅ |
| trace 裸字段跨单据歧义改反问 | 字段 key 在元数据里跨 ≥2 单据定义且未指定 form_key 时，`trace` 直接反问（`status:"need_clarification"` + `occurrences`），不再聚合列出全部单据证据；同单据内跨层级不受影响 | ✅ |
| `resolve_fields` 新增 `kind` 过滤参数 | 字段名/单据 key 同名（如 `cqkd_ht`）时三路候选混在一起噪声大；新增显式 `kind: "field"/"entity"/"form"`，`_items_for` 按 kind 只跑对应一路查询，从根上不产出另外两路噪声；CLI `resolve --kind` / MCP `tool_resolve_fields(kind=...)` 穿透 | ✅ |
| `tool_resolve_fields` docstring 把 `kind` 提前到开头强调 | 真实翻车：调用方只照工具描述前几句调用（只传 `keys`），`kind` 建议原来压在文末一大段之后没读到，导致能力已具备但没被用上；把"能确定标识种类就必须传 kind"连同三类触发场景的具体例子（单据号字面量/分录容器/字段读写）挪到 docstring 开头段，不改行为只改可发现性 | ✅ |
| 三段式限定符命中容器却误判 `mismatched_form` | 根因：容器命中（`entry`/`subentry`/`header`）item 只带 `parent_key`、没有 `entity_key` 键，旧版 `_matches` 统一用 `entity_key` 比较，容器命中永远判不匹配，命中的结果被回退进 `mismatched_form`；`_matches` 按 item 的 `kind` 分支，容器场景改用 `parent_key` 比较 | ✅ |
| 平台/继承字段特定单据下 mismatch 加 `note` 弱化措辞 | `auditdate` 类平台通用字段纯继承、零 override，该单据的 dym 里本就没有对应 `field` 元素，`resolve_fields` 找不到就报 mismatch，但这不是"限定符写错单据"；命中候选里存在 `field_kind in {platform,inherited}` 时 `mismatched_form[key]` 加 `note` 说明，渲染用 ℹ 而非 ⚠ | ✅ |
| `resolve_fields` `kind="entity"` 两段式分录限定符 fail-closed | 真实翻车：模型传两段式「分录.字段」（如 `cqkd_zdgl.cqkd_zdzfltk`）+ `kind="entity"`，无单据前缀时 `_matches` 不按单据过滤，若该坐标恰好只在别的单据下存在会返回看似钉准实则未经单据校验的候选；改为该分支直接拒绝，顶层新增 `invalid_request[key]`（`reason="missing_form_key"`+`entry_key`/`field_key`/`hint`），`resolved[key]` 给 `None`，强制改传三段式「单据.分录.字段」；三段式/字段侧两段式/裸 key/`kind` 不传时不受影响。MCP `tool_resolve_fields` 的 `kind` 同步改用 `Literal["field","entity","form"]` 类型标注 | ✅ |
| `resolve_fields` 新增 `kind="plugin"`：插件类名反查绑定单据 | 起因：`bill`/`trace` 的 instructions 明令"插件源码解读必须先核对绑定关系"，但只有插件类名（无 `form_key`）时工具箱没有任何一环能完成这次核对，模型被迫走命名惯例猜测；新增分支整串 `key` 当类名处理（跳过点号坐标限定符协议），`plugin` 表精确 `class_name` 命中为主，查不到且为裸简单类名时退化 Python 侧全表扫描按末段类名匹配，`plugin` 表零命中回落 `binding` 表（只标 `binding_status`/`confidence`/`source_relpath`，不臆造 `plugin_type`/`operation_key` 等元数据登记信息）；两表都查不到但 `source_class` 命中 `orphan_role='plugin'` 时给顶层 `unbound_in_source[key]`（诚实区分"钉不出绑定"与"钉不出类本身"两种 unknown）。CLI `--kind plugin`、MCP `tool_resolve_fields(kind="plugin")` 同步；不改 schema、不改 build | ✅ |
| `resolve_fields` `kind` 支持逐 key 列表 | 真实翻车：模型批量传入单据号/分录容器/字段三个不同层级的 key，却用单个 `kind="field"` 广播给全部三个，前两个全部落入 `mismatched_kind`（诚实报错但等于白跑一次）——根因是接口形状允许"整批一个 kind 值"套到分属不同层级的 key 上。`kind` 改为同时接受与 `keys` 等长的列表逐位对应（`None`/CLI `none` 占位表示该位不限定），长度不一致直接 `ValueError` 拒绝静默截断；单个字符串广播的老用法不变（整批同层级时仍适用）。CLI `resolve --kind` 改 `nargs="+"`、MCP `tool_resolve_fields(kind=...)` 类型标注同步扩展为 `Literal | list[Literal\|None] \| None` | ✅ |
| `read_source`（模式 A，含三档消歧/常量解析/表单兜底/防截断/游标提醒等 7 轮迭代） | 🗑 已退役（2026-07-05）——本地源码已是纯 UTF-8，"防乱码"前提不再成立；改走宿主自带 reader + `resolve_fields("实体key.字段key")` 精确核对。CLI `cosmic_kb source` 不受影响，富模式函数原样保留 | 🗑 |
| `method_calls`（方法出向调用导航） | 🗑 已退役（2026-07-05）——用户三轮追问证伪设计动机，唯一窄场景（通配符 import/继承方法）用通用 Grep/Read 即可解决，维护成本不划算，整体删除 | 🗑 |
| 隐藏坑 #1 · `trace --kind operation` 操作坐标追踪 | 场景"这单没人碰操作怎么执行了"。呈现二次拍板（2026-07-15，用户指出摊进 bill/trace 无针对性且冗余）：入站明细整合进 **trace 操作坐标**按需查询——新模块 `report/op_trace.py`，`trace "单据.操作key" --kind operation`（MCP `trace(field, kind="operation")`，坐标判别**纯显式**不自动猜测；裸操作 key 唯一命中自动定位、跨单据反问）返回三段：`triggered_by`（谁触发的，附 `caller_forms`=上游单据，递归 trace 即拼 A→B→C 跨单据链）/ `unresolved_inbound`（**无法静态排除是本操作**的嫌疑全量，2026-07-15 二次整合：目标=本单据但操作 key 真正解不出 `op_unresolved`、操作 key 匹配但**目标单据解不出** `target_unresolved`——表单插件绑多张单/目标动态拼接的外发即此形态，并入后查"谁调了该操作"trace 一次即完整、不再需要补查 `bill.outbound_triggers`、双边解不出 `both_unresolved`，每条带 `suspect_reason` 按强弱排序；只要 op key 已解出且≠所查操作（无论是否进入元数据操作集），或目标钉到别的单据，就必须静态排除）/ `triggers_downstream`（本操作插件又对外触发谁，每条带 `next_trace` 下一跳坐标）；操作不在元数据操作集但有同 key 触发点时照给证据+注明不可考，不得串成其他操作的嫌疑。紧凑投影三段皆可 cursor 翻页取全（cap 可取回纪律）。`bill` 只留**最小发现性信号**：`operations[].programmatic_trigger_count` + `stats.programmatic_trigger_count`/`unresolved_trigger_count`，外发 `outbound_triggers` 精简节保留（定位收窄为单据级**影响面**视图，不再是查触发链的必查补充）；字段 `trace` 只在 note 点名操作坐标+计数提示转查。`render_bill`/MCP docstring/返回值字段词典同步 | ⬜ |

**验收命令**：`cosmic_kb trace "单据.字段"` / `cosmic_kb trace "单据.操作" --kind operation` / `cosmic_kb bill "<单据标识>"` / `cosmic_kb resolve "<标识> ..."`

---

## 九、信任优先 · 可信度审计（coverage / scan-compare，CLI-only）

| 功能点 | 增强内容 | 状态 |
|---|---|---|
| 可信度报告（手段一+手段二） | `coverage`（元数据为分母算覆盖率+质量分解）+ `scan-compare`（粗扫vs高精度找盲点）；schema v8 | ✅ |

**验收命令**：`cosmic_kb coverage` / `cosmic_kb scan-compare`

---

## 十、语义解析层 semantic（词典底座，`ask` 已退役）

| 功能点 | 增强内容 | 状态 |
|---|---|---|
| 阶段 9 · 语义解析 + `ask`（NL→意图→确定性证据包）+ `plugin_explain`/`operation_explain` | 🗑 已退役（2026-07）——用户实测发现意图分类器对"类名.方法名"这类精确输入误判、需额外反提示才能纠正路由，而宿主大模型本来就比关键词分类器更擅长选工具；改为让宿主直接判断该调 `trace`/`bill`/`resolve_fields` 里的哪个，标识不精确时先用 `resolve_fields` 核对或自己读源码。连带整删 `semantic/resolver.py`、`context/` 整个包；`semantic/dictionary.py` 只保留 `resolve_fields` 仍用的精确 key 查询（`fields_by_key`/`entities_by_key`/`form_by_key`），删掉仅服务 ask 的 `OpEntry`/`ClassEntry`/`Candidate`/`fuzzy_*`（连带 `rapidfuzz` 可选依赖一并去掉） | 🗑 |

---

## 十一、MCP 封装 mcp（段二大模型接入）

| 功能点 | 增强内容 | 状态 |
|---|---|---|
| 阶段 10 · MCP 封装 | FastMCP + 取证工具，返回值与 CLI `--json` 同口径；stdio + `.mcp.json` | ✅ |
| 自包含包重构 | `importlib.resources` 定位资产，支持 `pip`/`uvx` 非 `-e` 分发 | ✅ |
| MCP 工具面精简后补 callers（当前 5） | 审计工具（coverage/scan-compare/dynwrites）下沉 CLI-only；`read_source`/`method_calls`/`ask` 整体退役；阶段 12.3 新增确定性 Java 反查 `callers`。当前工具面：`trace`/`bill`/`resolve_fields`/`callers`/`cosmic_semantics` | ✅ |

> ⚠️ MCP server 常驻，改 MCP/取证源码后需**重连/重启 MCP** 才生效。

**验收命令**：`cosmic_kb mcp --db cosmic_kb.db`

---

## 十二、分发与多 agent 接入（对话式安装）

> 依据 `docs/设计方案/分发与多agent接入方案.md`（2026-07-14 主路线：自研 bootstrap + 版本固定安装口令，首版 Windows/公开 PyPI/用户级隔离运行时）。

| 功能点 | 增强内容 | 状态 |
|---|---|---|
| Skill 分发（一期） | `cosmic_kb skill install/status/uninstall`：把 `cosmic-kb-understand`/`cosmic-kb-setup` 两份 SKILL.md 幂等装进 CodeBuddy/Qoder/TRAE（TRAE 生成导入包 + 设置页引导）；原子写、sha256 状态比对、目录 link 安全 | ✅ |
| 语义下沉进 MCP | server `instructions`（三态纪律 + 双场景路由）+ `WHEN_TO_USE` 路由表 + `cosmic_semantics(topic)` 按需回传单篇语义文档——非 Claude agent 仅注册 MCP 即带完整段二纪律 | ✅ |
| 中文名来源硬约束 | 真实调用中宿主大模型仍会拿源码注释/常量名当中文名、跳过 `resolve_fields`（等被质疑才补）。三层加固：① understand SKILL 点名「源码注释/常量名/Javadoc 不是元数据证据」+ 读完源码写结论前批量核对设为**强制门**；② 输出级约束——模板中每个中文名必须来自 MCP 返回，核对不了只准写 `<标识>（⚠️未核对）`（完成检查同步改为可机械自查条款）；③ 同一纪律下沉进 MCP `INSTRUCTIONS` 常驻段（覆盖非 Claude 宿主）。测试：`test_understand_skill_enforces_name_provenance` + `test_mcp_descriptions_keep_routing_contract_and_schema` 增补断言 | ✅ |
| D1 · 打包地基收敛 | 新增 `complete` extra（自引用聚合 parse/encoding/mcp/postgres，对话式安装口令固定装它）；彻底删除 fuzzy 引用（模糊解析器已 2026-07 退役，避免 `pip install [...,fuzzy]` 报未知 extra）；修 `_assets` 里不存在的 `adv/` 组 docstring 漂移；新增 `WHEN_TO_USE` 主题防漂移测试 | ✅ |
| D2 · MCP 自动注册 + 五工具校验 | `cosmic_kb/bootstrap/mcp_register.py`：`register` 幂等写共享 `.mcp.json`（CodeBuddy/Qoder 合并去重、原子写、同名冲突默认停止/`--force-mcp` 先备份再替换、保留他人 server）+ TRAE 导入包；`verify_mcp` 子进程真起 `cosmic_kb mcp --db` 完成 `initialize`+`tools/list`，断言 trace/bill/resolve_fields/callers/cosmic_semantics 五工具在列（仅写配置不算成功） | ✅ |
| D3 · Bootstrap 编排器 | `cosmic_kb/bootstrap/orchestrator.py` + `cli bootstrap plan/apply/status`。plan 只读探测（环境/候选/已有产物/冲突 + questions/actions，零副作用）；apply 按固定顺序幂等断点续跑（install.json→Skills→建KB→doctor→注册MCP→五工具校验→重连步骤，KB 已存在自动跳过）；`--prompt-db-password` 单进程 `getpass` 取口令只进环境变量、绝不落 JSON/清单/日志；`install.json`（runtime Python/CLI 绝对路径/版本/来源，无口令）为 setup skill 取 CLI 的唯一来源 | ✅ |
| D4 · 修 setup skill 自举 | 改 `cosmic-kb-setup/SKILL.md`：删「首次获得 cosmic_kb」职责（去掉旧「命令不存在时安装分发物」），改为先读 `%USERPROFILE%\.cosmic_kb\install.json` 取 `cli` 调用路径（不假设在 PATH）做已装环境的初始化/重建/升级/诊断/修 MCP；`install.json` 不存在时明确回「需用安装口令启动 Bootstrap」并停止、不自行安装；frontmatter 保持仅 `{name,description}`；understand 不动 | ✅ |
| D5 · 发版口令固定化 | `make_dist.ps1` 读到 `$Version` 后生成版本固定安装口令（内嵌 `cosmic-kb==<版本>` + 隔离 runtime `%USERPROFILE%\.cosmic_kb\runtime` + `[complete]` + bootstrap plan/apply 流程 + 口令终端隐藏输入约束），用 `INSTALL-TOKEN:START/END` 标记对纯字符串切片写回 README/安装说明.md（EOL 规整保证同版本幂等不脏树）；staging 兜底刷新覆盖 `-AllowDirty` | ✅ |

**验收命令**：`pytest -q tests/test_bootstrap.py tests/test_skill_installer.py tests/test_packaging_extras.py tests/test_install_token.py`；端到端手验见下方说明

---

## 十三、编译期符号解析 symbols（阶段 12）

> 依据 `docs/设计方案/跨类调用链解析与编译期符号方案.md`（2026-07-15 拍板删除"解析器绝不依赖
> 编译"红线并收窄到 `java/` 子包，spike 已验证 96.5%/96.7% 解析率）。**不执行任何构建**：
> 只读工程依赖声明（IDEA/.iml、金蝶官方 Gradle 模板）+ 绑定本地 jar（JavaParser Symbol
> Solver）；符号解不出退回 tree-sitter 名字匹配并如实标注来源（精度分级共存）。
> 拆 3 个子阶段：12.1 组件层（本表）→ 12.2 注入管线 + schema v18 → 12.3 call_edge + callers。
> 阶段 12 已全部完成；面向最终工具能力的整体变化、真实指标与使用边界见
> [`阶段 12 · 编译期符号解析与工具能力提升总结`](阶段12工具能力提升总结.md)。

| 功能点 | 增强内容 | 状态 |
|---|---|---|
| 12.1 · 类路径发现 | `java/symbols/classpath.py`：显式 `--classpath-dir` > IDEA（modules.xml→*.iml 兜底剪枝搜；库按 `<library name>` 索引非文件名，jarDirectory recursive 属性照传，`$PROJECT_DIR$/$MODULE_DIR$` 宏展开，module-library 内联库；损坏 XML/缺目录只 warning）> Gradle 模板（三级回退 `systemProp.cosmic_libs_path`→`cosmic_home` 拼 `/mservice-cosmic/lib`→环境变量 `COSMIC_HOME`；config.gradle 子目录表解析失败退固定五目录，outputdir 恒排除；settings.gradle 括号/语句双形 include，`project(':x')` 依赖边）> `status="none"`。attempts 诊断轨迹三条恒齐全（诊断是一等产物）。真实样本验收：IDEA 工程 3678 jar、Gradle 工程 4441 jar，与 spike 数字完全一致 | ✅ |
| 12.1 · JVM 微工具 symsolver | `tools/symsolver/`（Gradle 工程入 git）：javaparser-symbol-solver-core:3.25.10 + minimal-json，Java 8 兼容（本机构建 JDK 即 1.8，用 source/targetCompatibility 而非 options.release），fat jar 不用 shadow 插件（zipTree 合并，少一个插件门户依赖）。stdin 单 JSON → stdout JSONL（协议 v1，见设计方案§协议规格）；两层解析 expr→scope 兜底；编码由 Python 侧传、Java 按名解码 REPLACE 容错 + 剥 BOM；System.out 重定向 stderr 防第三方库污染协议流；单 jar/单文件/单调用点故障就地捕获（含 StackOverflowError）。产物 `cosmic_kb/java/vendor/symsolver.jar`（~5.7MB）git tracked 随包，`scripts/build_symsolver.ps1` 开发者侧重建 | ✅ |
| 12.1 · runner + SymbolTable | `runner.py`：find_java 三级探测（COSMIC_KB_JAVA→JAVA_HOME→PATH）`java -version` 10s 验活缓存；subprocess JSONL 流式消费（读线程+队列，Windows 管道无超时只能这么做）；看门狗两段阈值 600s（冷盘税实测 140s+/热盘 3~5s）/120s 静默；`RunOutcome(ok|partial|unavailable|failed)` 永不上抛，partial 已收部分照常可用。`table.py`：`(relpath,line,name)` 主键 + col 消歧「唯一→给/多条 col 命中→给/否则 None」诚实路径 + `stats()` 覆盖率分桶。真实样本全链复核：单文件 211 调用点覆盖率 96.2%，spike 锚点 `getZqzdGroupKey` 方法引用经 scope 兜底精确解析到 `ContractArrearsYSRQPlugin` | ✅ |
| 12.1 · doctor/打包接线 | `_assets.check_assets` 加 `symsolver` 资产行（jar 缺失=安装损坏）；doctor 新增「环境」段探测 java——**缺 java 只提示"符号解析将降级"不算体检失败**（软降级红线）；pyproject package-data 加 `cosmic_kb.java: vendor/*.jar`；make_dist.ps1 关键资产清单加 symsolver.jar；.gitignore 加 `.gradle/` | ✅ |
| 12.1 · 测试 | `test_symbols_classpath.py`（IDEA/Gradle/显式/attempts，fixture `tests/data/classpath_{idea,gradle}/` 0 字节占位 jar，纯文本解析无 importorskip）+ `test_symbols_runner.py`（FakePopen 回放 canned JSONL：col 消歧/method_reference argc=None/协议不符拒收/EOF→partial/看门狗击杀/find_java 败因可读/编码名翻译）+ `test_symsolver_integration.py`（真 JVM，无 java/无 jar 自动 skip：合成小工程秒级全链 + 真实样本 spike 锚点） | ✅ |
| 12.2 · AST 对齐 + 符号注入 | `Invocation` 新增字符列 `col` 与 `kind`；`iter_invocations(include_refs=True)` 补齐 `Class::method`，字段/常量消费者默认仍不扫无实参引用。`ProjectGraph`/反向调用图/事件 BFS/返回值追踪按 `ResolvedCall.source=local\|symbol\|heuristic` 传播；符号命中项目源码才进图，jar/jdk 已证实非 sink 的调用不再误充 unknown，failed/无 site 才退原启发式 | ✅ |
| 12.2 · sink/op_trigger 精度分级 | `find_sinks` 以声明类 FQN 白名单确认/否决（支持静态导入裸 `save` 与 `SaveServiceHelper::save`）；`op_trigger` 同口径确认 `OperationServiceHelper`，项目内同名类不再误报。`field_access.edge_source=local\|symbol\|mixed\|heuristic`、`operation_trigger.receiver_source=symbol\|text` 随 schema v18 落库并进入 trace/bill/coverage 返回 | ✅ |
| 12.2 · build 接缝/软降级 | CLI `build` 在 SQLite 事务前执行 discover→runner→SymbolTable，新增可重复 `--classpath-dir` 与 `--no-symbols`；bootstrap apply 原样透传。`kb_meta.symbol_resolution` 持久化 status/provider/jar_count/coverage/by_resolution/attempts/reason；coverage 展示符号状态与调用边四档。无 java 与坏 jar fixture 均能完成 v18 build，行数与 `--no-symbols` 基线一致 | ✅ |
| 12.2 · 测试/真实锚点 | 新增 `test_symbols_pipeline.py`：中文 UTF-8 字节列→字符列、method_reference 开关、跨类符号命中、平台同名否决、静态导入/method reference sink、反向传播 edge_source。全量 `pytest -q`：640 passed / 4 skipped；真实 IDEA 工程 1345 文件、3678 jar：86189 sites 中 83712 resolved，覆盖率 **97.13%**，0 文件失败；原翻车锚点 `ContractRefundUpdateAssetOp.afterExecuteOperationTransaction → ContractService::updateRlateAssets` 已进入跨类可达图。真实 KB 已用 `cosmic_db.json` + `--isv cqkd` 覆盖重建为 schema v18：`field_access=21763`，`edge_source` local 14327 / symbol 7436，`operation_trigger=102` 且全部由 symbol 确认，`PRAGMA integrity_check=ok` | ✅ |
| 12.3 · call_edge 持久化 | schema v19 新表 `call_edge` 全量保留类内/项目/平台边、`method_reference` 与 failed 站点；逐点落 caller/target/签名/行列/resolution/target_kind/confidence/evidence，target/caller 双索引。生成与字段分析复用同一棵 ProjectGraph，JavaParser/tree-sitter 对不齐的符号站点仍以 unknown caller 诚实落库 | ✅ |
| 12.3 · callers CLI/MCP | `report/callers.py` + `cosmic_kb callers "Class.method" --json` + MCP `callers`；简单类名跨包重名返回 `need_clarification`，方法引用与普通调用同口径返回。所有结果必附 `resolution_coverage`；0 结果仅在 symbol status=ok、无失败文件且覆盖≥95% 时标强证据，降级/低覆盖明确不足以断言死代码。MCP 按 28KB 字节预算游标分页，必须翻至 `pagination.complete=true`；server instructions 与 understand Skill 已补路由 | ✅ |
| 12.3 · 测试/指标验收 | `tests/test_callers.py` 覆盖 v19 表/双索引、四档边、CLI/MCP 同口径、跨页取全、简单名歧义、0 结果强弱措辞与方法引用反查；全量 `pytest -q`：647 passed / 4 skipped。真实 v19：`call_edge=86189`（与符号 sites 等量），expr 83214 / scope 498 / failed 2477，method_reference 535；原翻车方法反查 2 处（含方法引用），测试类 `AjustAudiValidatorTest.main` 反查 0 且标强证据；新 MCP 子进程 tools/list 五工具齐全 | ✅ |
| 12.3+ · callers 入口可达性/死代码判定升级 | `report/entry_chain.py` 新增 `registration_status(conn, cls)`：反查单个插件类的元数据注册状态，六值 `status`（`registered_enabled`/`registered_disabled`/`registered_enabled_unknown`/`orphan_unregistered`/`orphan_unverifiable`/`no_registration_evidence`）；convert 插件按 `plugin.form_key`（转换规则 id）LEFT JOIN `convert_rule` 取规则级 `enabled`（`enabled_source="convert_rule"`，规则悬空归 unknown 档），非 convert 走 `plugin.enabled`（`enabled_source="plugin"`）；无绑定的孤儿插件类按 `plugin_kind` 分类，KB 有注册表的种类（form/list/op/writeback/convert）查无绑定→`orphan_unregistered`，KB 未接入的种类（task/webapi/workflow/print/import/validator/unknown）恒 `orphan_unverifiable` 并固定措辞"KB 未接入该类插件的注册配置表…无法确定是否平台注册"（三态诚实，红线 #4）。`report/callers.py` 新增 `entry_analysis(conn, fqn, method, coverage)`：复用 `entry_chains()` 反向 BFS 上溯到插件事件入口/边界类，按类去重后逐类反查 `registration_status`，给出 verdict 五值——`entry_reachable`（≥1 入口 `registered_enabled`，confirmed/likely 视链上是否有 heuristic 边）/`entries_inactive`（入口全 `registered_disabled`/`orphan_unregistered` 且无截断、符号层可用，likely，永不 confirmed）/`entry_unverifiable`（存在未知注册状态入口，或负向结论遇截断/符号层弱化被降级，unknown）/`no_entry_found`（追不到入口，`strong_zero_evidence` 且无截断→likely 否则 unknown）/`not_analyzed`（目标为 jar/jdk 平台类，不跑 BFS）；降级铁律：负向结论（inactive/no_entry_found）遇截断或符号层弱化必降级，不冒充完整死代码证据。`callers()`/`callers_compact()`/`render_callers()` 已接线：富投影带完整 `entry_analysis`；MCP 首页带压缩后的注册状态（bindings 截 8 条）+ 入口链（`entry_chain.slim_chains` 复用，截 3 条链/8 跳），后续分页页仅带 `{verdict,confidence,note}`，预算超限时先丢 `entry_analysis.chains`（`chains_omitted:true`）保留 verdict/entries；CLI 人读追加「入口可达性」段。无 schema 变更（维持 v20） | ✅ |

**验收命令**：`pytest -q tests/test_callers.py tests/test_callers_entry.py tests/test_op_trigger.py tests/test_symbols_classpath.py tests/test_symbols_runner.py tests/test_symsolver_integration.py`；
`cosmic_kb callers "ContractService.updateRlateAssets" --json`；`cosmic_kb doctor`（有/无 java 两态）；jar 重建 `scripts/build_symsolver.ps1`

---

# 待开发 / 已搁置

| 功能点 | 说明 | 状态 |
|---|---|---|
| 阶段 11 · 增量重扫 + GitNexus 增强 | 文件指纹增量重扫；接 GitNexus 补调用链/影响范围 | ⬜ 待开发 |

---

_对应里程碑：M1 信任地基(0–4) · 本地 Web(4.5) · M2 行为分析引擎(5–7 及持续加固) ·
M3 AI 问答(9–10) · M4 增强(11)。_
